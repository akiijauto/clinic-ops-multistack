# レーンR の進捗

## 2026-09-05 17:55 — 1巡目：計画そのものの点検

書き出し先: `coordination/review/2026-09-05_計画レビュー.md`（15件）

**重い4件**: R-01（24画面/26画面の食い違い・売上集計が領域表に無い）、
R-02（ログインを「作る」と「扱わない」が両方書いてある）、
R-03（ポート未決でRailsとNext.jsが既定衝突）、R-04（共通テストをレーンが走らせる手段が無い）。

## 2026-09-05 18:45 — 2巡目：凍結後の契約点検＋3レーンを実際に起動して確認

書き出し先: `coordination/review/2026-09-05_2巡目.md`（新規4件＋1巡目の追跡）

**1巡目の追跡（要約）**:

- R-01・R-02・R-03・R-07・R-15 は**解消済み**（各レーンが自分で気づいて仮決めし、
  `qa/` に記録していた。私が指摘するより先に動いていたものもある）
- R-05・R-06・R-10・R-12・R-14 は**未修正のまま**

**新規（2巡目）**:

| # | 何が | 重さ |
| --- | --- | --- |
| R-16 | 病院設定の6項目のうち**4つ**（ポイント・最終伝票番号・機関コード・ロゴ）が、
  `spec/model.md` の `Clinic` にも「落としたもの」表にも無い。**契約自身が禁じている
  「落としたものを黙って落とす」状態になっている** | 重い |
| R-17 | DM画面のCSV書き出しに**文字コードの規則が無い**（5実装で割れうる） | 中 |
| R-18 | Laravel/FastAPI/Next.jsを**実際に起動**して共通テストを実行 → **3件とも実測で通過**。
  各レーンの「通った」報告は嘘ではなかった | 確認（良い知らせ） |
| R-19 | レーンE `npm test` は今も `BASE_URL` 無しだと2件をskipして緑（R-14と同一の未修正） | 中 |

**確認のために自分で起動したFastAPI・Next.jsの2プロセスは、確認後すぐに停止しました**
（レーン本体の起動とポートが衝突しないように）。

## この役自身の制約（R-06、未解決）

「テストをわざと壊して落ちることを確かめる」は、テスト・実装とも変更禁止・
テストランナー無許可のままでは実行できません。1巡目で提案を書きましたが、
**指揮役からの応答はまだありません。** 待っている間、実測できる範囲
（起動して共通テストを叩く・契約の記述を突き合わせる）で代替しています。

## 次に見るもの

- `data-check` 属性が実際にテンプレートへ出ているか（画面に嘘が無いか、本題）
- `qa/` の仮決め同士が矛盾していないか
- 検算1〜9の報告が出てきたら、自分で再現する
