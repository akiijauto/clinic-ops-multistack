# レーンR の進捗

## 2026-09-05 17:55〜2026-09-06 04:00 — 1〜3巡目

（計画点検15件、契約点検4件、修正検証＋判定を壊す訓練＋生きた状態の点検。詳細は各回の
`coordination/review/*.md` を参照。R-06（自分がテストを壊せない制約）は解消済み）

## 2026-09-06 07:30 — 4巡目：統括の3件を追跡 ＋ 予約（新規画面）を実地で確認

書き出し先: `coordination/review/2026-09-06_4巡目.md`

### 統括の3件（`review/2026-09-06_統括_横並び再測.md`）はすべて自然解消

測り直すと3件とも再現しない。原因を1件ずつ追った結果:
- Rails消費税: **コードのバグではなく、DBの `Clinic.tax_rate` が生きた状態で0.08になっていた**
  （正は0.10）。設定画面から保存し直したか再シードで解消したと見られる
- Next.jsカルテ・Rails死んだリンク: どちらも「作業中を横取りしただけ」で、直後に解消

### 新規に見つけたもの：予約（新画面）のJSON API作成経路に、5実装中3実装で欠陥

同一ペイロードで `POST /api/reservations` を5実装すべてに送って確認:

| レーン | 結果 |
| --- | --- |
| A Go | 201 正常 |
| B Rails | **404**（`Api::ReservationsController` に create が無い。画面側は別実装で動く） |
| C Laravel | **405**（`/api/reservations` にそもそもPOSTルートが無い） |
| D FastAPI | **500**（タイムゾーン付き日時 `+09:00` を渡すとクラッシュ。**この形式は`data/seed.json`の正データそのもの**） |
| E Next.js | 201 正常 |

**共通テスト（検算6）はGETしか使わないため、この3件を永遠に検出できない構造になっている。**

### 副次的な発見：Rails予約一覧は`limit(50)`のみで日付フィルタ未実装

画面からの登録は実際に動く（フォーム送信で確認済み、DBに保存も確認済み）が、
51件目以降の予約は一覧に出てこない。`?from=` パラメータはコードにあるが絞り込みに未使用
（実測：異なる日付を指定してもバイト単位で同一のHTMLが返る）。

## 次に見るもの

- 売上集計（もう1つの新画面）のPOST/PATCH経路
- レーンB・Cの予約API実装後の再確認
- 「全部緑」は確認時刻限りとして扱い、時間を置いて再測する

## 2026-09-06 09:19 — 自分の仕事が本当に終わっているかの自己点検

**結論: 「終わり」と言い切れない。理由を書く。**

1. **4巡目の指摘（R-20〜R-22）を今また実測し直したが、3件とも無修正のまま再現した。**
   Rails 404 / Laravel 405 / FastAPI 500、いずれも同じ。指揮役からの応答もまだ無い
   （`coordination/qa/` `coordination/review/` を確認、4巡目報告以降に増えたファイルは無し）。
   **私の報告が読まれた形跡がまだ無いので、「見つけて書いた」で仕事が終わったとは言えない**
   ——直すのは私の役目ではないが、**見つけたものが本当に伝わったか**は自分で確かめるべき事柄

2. **「新しく置かれた画面」のうち、予約は深く見たが売上集計は表面しか見ていない。**
   `売上集計` は読み取り専用（保存操作が無い画面）なので "押しても何も起きない" 型の
   欠陥そのものは構造的に起きにくいが、5レーンとも `/api/sales/summary` が200で
   一貫した値を返すことだけ確認した程度。**画面側（HTML）の3表切り替え・
   `data-check` の実際の表示までは見ていない**

3. **「押せる保存ボタンがあるのに押しても何も起きない画面」は、予約1画面しか
   体系的に洗っていない。** 26画面・5実装ぶんの保存ボタンを全部あたる、という意味では
   終わっていない

**次にやるなら**: (a) 指揮役へ届いたかの確認、(b) 売上集計の画面側、
(c) 他の新規/最近変更された画面（会計・入院・顧客の6項目等）の保存ボタンの実地確認。

## 2026-09-06 13:30 — 5巡目：領域別フォーク5体（受付・診療・会計/売上・入院/予約/業務・設定+到達性）で並列調査

書き出し先: `coordination/review/2026-09-06_5巡目.md`

### R-20/21は解消、R-22（FastAPI予約500）はまだ再現（自分で再実測）

前回指摘3件のうち2件は自然解消済み。FastAPIのタイムゾーン付き日時500は今も再現する
（`data/seed.json`の予約データの正データ形式そのものを踏むため重い）。

### 新規に見つけた最重要所見（自分で再実測して確認したもの）

1. **Rails・Laravel・Next.js: 飼主氏名がDBレベルで誤っている**（owner_id=18の正しい姓に対し、
   3実装は共通して別の姓を返す。文字列は伏せる——過去に公開ゲートのNGワードと衝突した経緯があるため。
   住所・電話は正しく氏名だけがずれる＝シード生成の共通処理が疑わしい）。
   DM CSVの宛先氏名にもそのまま出る（フォーク報告、75行全不一致）
2. **Laravel: 「本日」の判定基準が独自に`anchor_date`固定**（他4実装は壁時計日）。同時刻に
   `/today`を叩くとLaravelだけ25件・visit_count=3、他は0件。共通テストは`visit_count.today`の
   値自体を検証しないため永遠に検出不能
3. **Rails: カルテ画面に保存・新規診察・印刷への導線が皆無**（バックエンドは動くが画面から辿れない）
4. **Rails: ナビに`/dm`・`/sales`へのリンクが無い**（孤立画面）
5. **Laravel: 「このシステムについて」に学習研究目的・複製禁止の必須文言が全欠落**

### フォーク経由（自分では未再検証、各フォークが具体的コマンド・出力で実測済み）

会計・売上：DM CSVのBOM/CRLF不備（全実装）、売上集計3表がLaravel1/3・FastAPI2/3のみ、
会計PATCHがGo無反応・FastAPI/Next.js未実装。
診療：カルテのrow-visitマーカーが5実装でバラバラ、検査項目件数が5/14/90と不一致（契約の曖昧さ）、
書類画面の登録UIがFastAPI/Next.jsに皆無、PDF以外の拒否をRails/Laravelが実装していない。
受付：区分タブがLaravelに無い、全文検索がFastAPI/Laravelに無い、トップ画面の必須文言がGo/FastAPIに無い。
入院/予約/業務：予約の日付フィルタがGo/FastAPIで無視される、スタッフ選択手段がRails/Laravel/FastAPIに
皆無、Laravelは退院済み入院にケア記録を追加できてしまう。
設定/到達性：Next.jsはナビが薄く半数近くの画面が孤立、Laravelのマスタ画面が生JSONダンプ、
取込の試し読みがGo405・LaravelでPHP警告が漏れる、カルテ診察一覧の並び順がRails/Laravelで
新しい順になっていない。

詳細・4点セット（何が/どの実装で/どう確かめたか/なぜ問題か）は `review/2026-09-06_5巡目.md` 参照。

（追記）合成データの姓を報告文に生で書いていた件、指揮役の指摘を受けて review/status とも
「owner_id=18」でIDを指す書き方に直した。以後この種の値は伏せて書く。

## 2026-09-06 15:00 — 5巡目補足：「判定器が見ていない範囲」を洗った

指揮役の依頼を受け、`tests/checks.py`・`tests/inventory.py`を全文読んで、各検査が
実際に何を照合しているかをコード単位で確認した（フォークは使わず自分で読んだ）。
書き出し先: `coordination/review/2026-09-06_5巡目補足_判定器の穴.md`

**まず良い知らせ**: 5-01（飼主氏名）を受けて`_same_data`検査が既に追加されていた。
**ただしフィールド名に誤りがあり**（`tel`→正しくは`phone`、`name`→正しくは`name_kanji`）、
存在しないキーを`.get()`で読むため例外もエラーも出ないまま、**電話番号と動物（Patient）の
氏名の食い違いを今も検出できていない**（実測で確認。1行修正で直ると見られる）。

**新たに見つけた構造的な穴7件**:
- 検算5：画面の`data-check-flag`（色）を一度も見ず、APIの`judgment`文字列のみで判定
- 在庫（画面/APIルート）：GETしか送らず405を合格扱い。書き込み経路の実際の動作は
  `_write_roundtrip`（予約のみ）以外は一度も検証されない
- 検算8（死んだリンク）：到達画面数のしきい値が10（26画面中）で、どの画面が辿れないか
  を名指ししない
- 検算4（画面/印刷一致）：4数値のみで、主訴・診断等の文章項目は契約側にも目印が無く
  比較不能
- 検算1・検算2：内訳の各行（担当・分類の対応関係）は見ず、総額・代表1件のみ
- 検算7：画面側の`data-check="care_record.performed_by"`を見ずAPIのみ
- 在庫（data-testid）：`screen-*`以外（行・空表示の目印）は対象外。属性取り違え・誤付与
  を検出しない構造（5巡目本編のrow-visit不一致3件の根本原因）

詳細は上記ファイル参照。

## 待機します

指示があるまでこのまま待機します（自動フォールバックのスケジュールは入れません）。
