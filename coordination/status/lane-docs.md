# レーンDOCS の進捗

## 2026-09-06 12:00 — README.md / coordination/実績-2026-09-06.md を実測して更新

**担当範囲**: `README.md` と `coordination/実績-2026-09-06.md` のみ。実装・契約・
判定器は読むだけで、一切書き換えていない。コミット・pushもしていない。

### やったこと（すべて自分で実測してから書いた）

1. **5サーバーの疎通確認**（`curl /healthz`）: Go 8401 / Rails 8414 / Laravel 8403 /
   FastAPI 8415 / Next.js 8405、全部 `{"status":"ok"}`
2. **`tests/run.py` を5サーバー全部に個別実行 ＋ `tests/compare.py`**: 22件×5実装
   すべてOK、食い違い0（実行時刻: 11:20〜11:47頃）
3. **行数を測り直した**（`git ls-files` 起点、依存の実体・lockファイル・
   コンパイル済みバイナリを除外し `wc -l`）。詳しい基準は `実績-2026-09-06.md` に明記
4. **`data/seed.json` を直接読んで `papers` キーの不在を確認**（在庫検査の
   「確かめられない」が嘘でないことの裏取り）
5. **`POST /api/reservations` を5実装へ実際に送った**（11:48〜11:49、契約どおり
   タイムゾーン付き日時 `+09:00` を含む payload）。FastAPIのみ500を再現。
   `coordination/review/2026-09-06_4巡目.md`（レーンR、07:30）と
   `coordination/review/audit-指揮役.md`（レーンS、11:20〜11:50）で先に報告
   済みの欠陥と一致することを確認したうえで、**自分でも同じ時刻帯に再現**
6. **`/papers/1` を2つの時刻（11:41のレーンS実測と11:49の自分の実測）で比較**し、
   どの実装が200を返すかが変わることを確認（起動来歴に依存し再現しない）

### 見つけたもの（このレーン独自）

- **`stacks/go/clinicops.exe` がgit追跡されている。** コンパイル済みバイナリで、
  `wc -l` に通すと65,487「行」という無意味な数字が出る。行数集計から除外し、
  `実績-2026-09-06.md` に「リポジトリの手入れ漏れ」として記録した
  （削除はこのレーンの担当範囲外なので行っていない）

### 書いたもの

- `README.md`: 「実測」セクションを22件版に更新、「途中で見つかったもの」に
  2026-09-06の3件（テストが緑=完了と誤読／判定器の欠陥6件中3件は統括役が作成／
  多数決は正しさの証明にならない）を追加、「どこまで作ってあるか」を実測ベースで
  全面更新（75/78確認・papers3件は測るたびに結果が変わる・POST /api/reservations
  でFastAPIが500を返す、を明記）
- `coordination/実績-2026-09-06.md`: 2026-09-05時点の記述は残したまま、
  2026-09-06時点の行数・見積もり差・「何が言えるか／言えないか」を追記。
  末尾に「今日いちばんの学び」を新設し、指揮役の反省・rulingsの骨子を
  要約しすぎずに書いた

### 明示的に「未確認」と書いた事項

- 判定器の欠陥1〜3件目（誰が作ったか）——`rulings.md`に記載が無い
- 統括役の判断ミスのうち「500を在るに数えた」件——誰が最初に気づいたか判然としない
- 行数がなお計画の半分以下である理由（生産性差か実装密度か）

## 待機します

指示があるまでこのまま待機します（自動フォールバックのスケジュールは入れません）。
