# 5実装の横並び実測（指揮役）

**同じ judge・同じ問い・同じデータ**で測った。自己申告ではなく指揮役が自分で走らせた値。

## 2026-09-05 23:45頃 — smoke（2件）

| レーン | スタック | 判定 | 応答 |
| --- | --- | --- | ---: |
| A | Go | **全2件 通過** | 16ms |
| B | Rails | **全2件 通過** | 29ms |
| C | Laravel | **全2件 通過** | 184ms |
| D | FastAPI | **全2件 通過** | 1ms |
| E | Next.js | **全2件 通過** | 9ms |

**5実装すべてが同じ judge を通った。** Next.js は最初の計測時に接続できなかったが、
レーンEが作業中でサーバを入れ替えていたためで、実装の失敗ではなかった。

### レーンEが直したこと（レーンRの指摘 R-14 / R-19）

`npm test` が `BASE_URL` 無しのとき2件を **skip して緑**になっていた。
既定値を使い、サーバが無ければ落ちる形へ変更。実測:

```
✖ GET /healthz returns {"status":"ok"} — ECONNREFUSED
ℹ tests 24 / pass 22 / fail 2 / skipped 0
```

**skipped が 0** になった。「通った」と「試していない」が区別できる。

### 応答時間について

**この数字を「速さの比較」として使わないこと。**
`/healthz` を1回叩いただけであり、起動直後かどうか・JITが温まっているか・
サーバの形（開発用か本番相当か）がバラバラである。

比べるなら、**同じ条件を揃えてから測り直す**。第3段階の突き合わせで行う。

## 次の段階

4レーン（A・B・C・D）へ money 組（検算1・2）を渡した。

```
検算1 売上が分類別・担当別・日別・総合計で一致する
検算1 分類別の構成比の和がちょうど100.0%
検算2 単価未設定の行を0円で合計に入れず、未算入の行数を出す
検算2 消費税は伝票単位で1回だけ切り捨て
```

期待値（`python tests/expected.py`）: 税抜売上 **5,185,704円**、未算入 **12行**。
