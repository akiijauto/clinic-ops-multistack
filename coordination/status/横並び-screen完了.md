測る対象: A(Go:8401), B(Rails:8402), C(Laravel:8403), D(FastAPI:8414)
止まっている: E(Next.js:8405)
  ※ 止まっているものは**「合格」にも「不合格」にもしない**。測っていないだけ。

| 検査 | A | B | C | D |
| --- | --- | --- | --- | --- |
| GET /healthz が 200 で {"status":"ok"} を返す | OK | OK | OK | OK |
| 起動していて、応答が返る | OK | OK | OK | OK |
| 検算1 売上が分類別・担当別・日別・総合計で一致する | OK | OK | OK | OK |
| 検算1 分類別の構成比の和がちょうど100.0% | OK | OK | OK | OK |
| 検算2 単価未設定の行を0円で合計に入れず、未算入の行数を出す | OK | OK | OK | OK |
| 検算2 消費税は伝票単位で1回だけ切り捨て | OK | OK | OK | OK |
| 検算2 この規則を、いまのデータで確かめられているか（検算そのものの点検） | OK | OK | OK | OK |
| 検算3 体温が全患者で同じ値になっていない | OK | OK | OK | OK |
| 検算4 カルテの画面と印刷で同じ値が出る | OK | OK | OK | OK |
| 検算5 基準の外にある値は判定欄と色の両方に出る | OK | OK | OK | OK |
| 検算6 予約が担当・処置室のどちらでも重ならない | NG | NG | NG | NG |
| 検算7 入院の記録行に実施者が必ず入っている | NG | NG | NG | NG |
| 検算9 削除済みは一覧から消えるが件数には残る | OK | OK | OK | OK |
| 検算8 画面から辿れるリンクが全部生きている | NG | NG | NG | NG |

全実装がそろって落ちている検査が 3 件。**まだ作っていないだけ**の可能性が高い。
