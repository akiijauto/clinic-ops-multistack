# レーンS の進捗（指揮役の監査）

## 2026-09-06 11:20〜11:50 実施

5つの主張を実測で検証した。書き込み先は `coordination/review/audit-指揮役.md`。

**いちばん重い指摘**: 「全通過・食い違い0」は事実だが（`tests/compare.py`で
5実装×22件OK・食い違い0を再現した）、検算6はGETしか送らないため
`POST /api/reservations`（予約新規作成）を一度も検査していない。実測すると
**FastAPI(8415)は同一payloadで500 Internal Server Errorを返す**（再現確認済み）。
これはレーンRが4巡目（07:30）で先に報告済みの穴で、Rails/Laravelは直っているが
FastAPIは指揮役への報告後も直っていない。

次に重いのは、「画面42・API36がすべて在る」が、同時に指揮役自身が認めている
「78件中3件は確かめられない（papers関連）」を暗に「在る」側へ含めてしまっている点。
実測（`/papers/1`）では5実装中4つが404で、papersの個別ページは今のサーバー状態では
確認できない。

CSSの中身一致（`spec/ui.css`とのdiff、5実装すべてでバイト単位一致）は実測で裏付けが
取れており、この部分の主張は正しい。「確かめられないのは78件中3件」という数字自体も
5実装すべてで再現し、正確だった。

詳細4行フォーマットの指摘は `audit-指揮役.md` に5件書いた（3件は根拠不足、1件はほぼ
正確、1件は範囲未定義）。実装ファイルは1行も変更していない。コミット・pushもしていない。

## 2026-09-06 11:50〜12:00 追記 — 判定器そのものの点検（指揮役からの依頼）

`spec/acceptance.md`の検算6・検算7の「どう確かめるか」を`tests/checks.py`の実装と
1行ずつ突き合わせた。**契約書が明記している合格条件の後半（能動的に拒否を確かめる手順）が
2件とも判定器に実装されていない。**

- 検算6（322-351行）: 「重複するAPI新規作成が拒否され件数不変」「キャンセル済み時間帯には
  新規作成できる」の2条件を`_reserve`は確認していない（`POST /api/reservations`を
  一度も送らない）
- 検算7（355-378行）: 「実施者を指定しない作成が拒否され件数不変」を`_care`は
  確認していない

実際に5実装すべてへ直接APIを叩いて確認したところ、**3条件とも5実装全て正しく動作していた**
（重複409拒否・キャンセル済み枠への新規作成成功・実施者なし422拒否）。**今日時点でのバグ
ではない。** ただし判定器にこの検査が無いため、今後の回帰は`tests/run.py`が全緑のまま
検出できない。検証に使った予約・記録データはすべて後始末済み（作成→確認→キャンセル）。

もう1件、`_reserve`単体の脆さとして、`GET /api/reservations`が空配列を返しても
「0件で重なり0」でOKになる（最低件数ガードが無い）ことも記録した。他の検算は軒並み
このガードを持つが検算6だけ持たない。

詳細は`coordination/review/audit-指揮役.md`の「6.」に追記済み。実装ファイル・
`tests/`は1行も変更していない。

## 待機します

指示があるまでこのまま待機します。
