# レーン再投入 — 結果

`data/` 作り直し（総合計 5,189,585円・未算入12行・基準日2026-09-06）を受けて、
5実装すべてに入れ直した。**`data/` `spec/` `tests/` は読むだけ。`stacks/` 配下のコードは1行も変えていない。**
コミット・pushはしていない。

## やったこと（実装ごと）

| 実装 | port | 投入コマンド | サーバ再起動 |
| --- | --- | --- | --- |
| Go | 8401 | データはファイルを直接読む実装（DBもマイグレーションも無し）。投入コマンド無し | プロセスkill→同じ`clinicops.exe`を再起動（再ビルド不要） |
| Rails | 8414 | `bundle exec rails db:seed`（1回目は謎のエラー、2回目で成功。`seeds.rb`は自前で全テーブル`delete_all`してから入れ直す作り） | プロセスkill→`bin/rails server -p 8414 -b 0.0.0.0` |
| Laravel | 8403 | `bash tools/php.sh artisan migrate:fresh --seed --force`（`tools/php.sh`が`PHPRC`をこのスタック専用php.iniに向けている。素の`php`だと`pdo_sqlite`が無く`could not find driver`で落ちる） | `cache:clear`実施＋プロセスkill→`bash tools/php.sh artisan serve --port=8403` |
| FastAPI | 8415 | 着手時点で**既に別エージェントが投入・再起動済み**だった（サーバ起動時刻17:21、受付が全件2026-09-06）。私はDBファイル削除を試みたが`Device or resource busy`で失敗→中身を確認したら既に新データだったので再投入はスキップ | 既に再起動済みだったので触っていない |
| Next.js | 8405 | `npm run seed`。**1回目は`UNIQUE constraint failed: clinic.id`で失敗**（`next dev`が稼働中でWALロック→`rmSync`が黙って失敗、README注意書きどおりの事故）。稼働中プロセスをkillしてから2回目で成功 | プロセスkill→`npm run dev -- -p 8405`。`data/clinic.db`のみ触った（`dev-area4.db`/`dev-area5.db`はコードからの参照が見当たらず、放置） |

Rails・Laravel・Next.jsはいずれも投入ログの件数が一致: `clinic=1 staff=10 owners=40 patients=60
receptions=25 visits=200 progress_notes=261 preventions=80 dosings=40 lab_tests=80
lab_test_items=401 billings=150 billing_details=600 reservations=60 hospitalizations=8
care_records=108`。

## 確認できたこと（依頼の3点）

- 検算1の総合計: **5実装とも 5,189,585円** で一致
- 検算2の点検: **5実装とも「150枚中132枚が丸め方を区別する（見張りとして働いている）」**
- 受付区分タブの「★確かめられていない」注記: **5実装とも出なくなった**
  （本日の受付が0件でなくなったため。ただし下記の新しい失敗が3件出た）

## `python tests/run.py` の結果

```
Go       (8401): 全 29 件 通過
Laravel  (8403): 全 29 件 通過
Next.js  (8405): 29 件中 1 件 失敗
Rails    (8414): 29 件中 1 件 失敗
FastAPI  (8415): 29 件中 1 件 失敗
```

### Go — 全29件通過

```
  OK  GET /healthz が 200 で {"status":"ok"} を返す
  OK  起動していて、応答が返る  — 1ms
  OK  検算1 売上が分類別・担当別・日別・総合計で一致する  — 4値とも 5,189,585 円
  OK  検算1 分類別の構成比の和がちょうど100.0%  — 和=100.0
  OK  検算2 単価未設定の行を0円で合計に入れず、未算入の行数を出す  — 伝票28 税抜49,528 未算入1行
  OK  検算2 消費税は伝票単位で1回だけ切り捨て  — 税2,753 税込30,285
  OK  検算2 この規則を、いまのデータで確かめられているか（検算そのものの点検）  — 150枚中 132枚 が丸め方を区別する（見張りとして働いている）
  OK  検算3 体温が全患者で同じ値になっていない  — 14 種 / 31 件
  OK  検算4 カルテの画面と印刷で同じ値が出る  — 20 組を比べて差なし
  OK  検算5 基準の外にある値は判定欄と色の両方に出る  — 50 項目の判定が一致
  OK  検算6 予約が担当・処置室のどちらでも重ならない  — 61 件で重なり0
  OK  検算7 入院の記録行に実施者が必ず入っている  — 108 件中 実施者なし 0 件
  OK  検算9 削除済みは一覧から消えるが件数には残る  — 一覧から消えても集計に残る
  OK  検算8 画面から辿れるリンクが全部生きている  — 60 画面を辿って切れなし
  OK  在庫 契約の画面ルートが全部ある（未実装はクローラーに見えない）  — 39/42 件ある（3 件は確かめられない: /folded/{key}, /papers/{paper_id}, /papers/{paper_id}/remove）
  OK  在庫 契約のAPIルートが全部ある  — 35/36 件ある（1 件は確かめられない: /api/papers/{paper_id}）
  OK  在庫 この検査自体が働いているか（確かめられない分が多すぎないか）  — 対象 78 件中、確かめられないのは 4 件
  OK  在庫 契約が求める data-testid が画面に出ている  — 34 画面で目印を確認
  OK  在庫 画面でもAPIでもないルートが応答する（CSV配信・死活・外部照会）  — 3/3 件が応答
  OK  見た目 共通CSS(/ui.css)を配っていて、全画面が読んでいる  — 34 画面すべてが読んでいる
  OK  見た目 指示したクラスが実際に画面に付いている  — 34 画面で 3 クラスすべてを確認
  OK  契約 押しても何も起きないボタンにしない（灰色3つが理由へ繋がる）  — 3 個: done, done_all, temp_save
  OK  書き込み 作って・確かめて・取り消せる（GETだけでは分からない）  — 作成→取消の往復ができた（id=62）
  OK  データ 実装が読んでいる中身が data/ と一致する（数字だけ見ても分からない）  — 513 項目が data/ と一致
  OK  見た目 トップの見出しと共通ナビが契約どおり（5実装で同じに見えるか）  — 32 画面で見出しとナビを確認
  OK  見た目 トップの本文が契約どおり（ナビの複製を並べない）  — 説明あり・本文のリンク 2 本
  OK  契約 本日の患者に受付区分のタブがある  — 6 区分すべてにタブがあり、既定は「初診」1 行（data/ から独立に数えた値と一致）
  OK  見た目 画面名がナビの表示と同じ（同じ画面を別の名前で呼ばない）  — 9 画面の名前がナビと一致
  OK  見た目 ナビに無い画面の名前も契約と同じ（患者名を見出しに混ぜない）  — 13 画面の名前が契約と一致

全 29 件 通過
```

### Laravel — 全29件通過

```
  OK  GET /healthz が 200 で {"status":"ok"} を返す
  OK  起動していて、応答が返る  — 545ms
  OK  検算1 売上が分類別・担当別・日別・総合計で一致する  — 4値とも 5,189,585 円
  OK  検算1 分類別の構成比の和がちょうど100.0%  — 和=100.0
  OK  検算2 単価未設定の行を0円で合計に入れず、未算入の行数を出す  — 伝票28 税抜49,528 未算入1行
  OK  検算2 消費税は伝票単位で1回だけ切り捨て  — 税2,753 税込30,285
  OK  検算2 この規則を、いまのデータで確かめられているか（検算そのものの点検）  — 150枚中 132枚 が丸め方を区別する（見張りとして働いている）
  OK  検算3 体温が全患者で同じ値になっていない  — 14 種 / 31 件
  OK  検算4 カルテの画面と印刷で同じ値が出る  — 20 組を比べて差なし
  OK  検算5 基準の外にある値は判定欄と色の両方に出る  — 50 項目の判定が一致
  OK  検算6 予約が担当・処置室のどちらでも重ならない  — 50 件で重なり0
  OK  検算7 入院の記録行に実施者が必ず入っている  — 108 件中 実施者なし 0 件
  OK  検算9 削除済みは一覧から消えるが件数には残る  — 一覧から消えても集計に残る
  OK  検算8 画面から辿れるリンクが全部生きている  — 60 画面を辿って切れなし
  OK  在庫 契約の画面ルートが全部ある（未実装はクローラーに見えない）  — 40/42 件ある（2 件は確かめられない: /papers/{paper_id}, /papers/{paper_id}/remove）
  OK  在庫 契約のAPIルートが全部ある  — 35/36 件ある（1 件は確かめられない: /api/papers/{paper_id}）
  OK  在庫 この検査自体が働いているか（確かめられない分が多すぎないか）  — 対象 78 件中、確かめられないのは 3 件
  OK  在庫 契約が求める data-testid が画面に出ている  — 36 画面で目印を確認
  OK  在庫 画面でもAPIでもないルートが応答する（CSV配信・死活・外部照会）  — 3/3 件が応答
  OK  見た目 共通CSS(/ui.css)を配っていて、全画面が読んでいる  — 36 画面すべてが読んでいる
  OK  見た目 指示したクラスが実際に画面に付いている  — 36 画面で 3 クラスすべてを確認
  OK  契約 押しても何も起きないボタンにしない（灰色3つが理由へ繋がる）  — 3 個: karte_temp_save, today_complete_delete_all, today_complete_delete_one
  OK  書き込み 作って・確かめて・取り消せる（GETだけでは分からない）  — 作成→取消の往復ができた（id=62）
  OK  データ 実装が読んでいる中身が data/ と一致する（数字だけ見ても分からない）  — 513 項目が data/ と一致
  OK  見た目 トップの見出しと共通ナビが契約どおり（5実装で同じに見えるか）  — 34 画面で見出しとナビを確認
  OK  見た目 トップの本文が契約どおり（ナビの複製を並べない）  — 説明あり・本文のリンク 2 本
  OK  契約 本日の患者に受付区分のタブがある  — 6 区分すべてにタブがあり、既定は「初診」1 行（data/ から独立に数えた値と一致）
  OK  見た目 画面名がナビの表示と同じ（同じ画面を別の名前で呼ばない）  — 9 画面の名前がナビと一致
  OK  見た目 ナビに無い画面の名前も契約と同じ（患者名を見出しに混ぜない）  — 13 画面の名前が契約と一致

全 29 件 通過
```

### Rails — 29件中1件失敗

```
  OK  GET /healthz が 200 で {"status":"ok"} を返す
  OK  起動していて、応答が返る  — 56ms
  OK  検算1 売上が分類別・担当別・日別・総合計で一致する  — 4値とも 5,189,585 円
  OK  検算1 分類別の構成比の和がちょうど100.0%  — 和=100.0
  OK  検算2 単価未設定の行を0円で合計に入れず、未算入の行数を出す  — 伝票28 税抜49,528 未算入1行
  OK  検算2 消費税は伝票単位で1回だけ切り捨て  — 税2,753 税込30,285
  OK  検算2 この規則を、いまのデータで確かめられているか（検算そのものの点検）  — 150枚中 132枚 が丸め方を区別する（見張りとして働いている）
  OK  検算3 体温が全患者で同じ値になっていない  — 14 種 / 31 件
  OK  検算4 カルテの画面と印刷で同じ値が出る  — 20 組を比べて差なし
  OK  検算5 基準の外にある値は判定欄と色の両方に出る  — 50 項目の判定が一致
  OK  検算6 予約が担当・処置室のどちらでも重ならない  — 61 件で重なり0
  OK  検算7 入院の記録行に実施者が必ず入っている  — 108 件中 実施者なし 0 件
  OK  検算9 削除済みは一覧から消えるが件数には残る  — 一覧から消えても集計に残る
  OK  検算8 画面から辿れるリンクが全部生きている  — 60 画面を辿って切れなし
  OK  在庫 契約の画面ルートが全部ある（未実装はクローラーに見えない）  — 40/42 件ある（2 件は確かめられない: /papers/{paper_id}, /papers/{paper_id}/remove）
  OK  在庫 契約のAPIルートが全部ある  — 35/36 件ある（1 件は確かめられない: /api/papers/{paper_id}）
  OK  在庫 この検査自体が働いているか（確かめられない分が多すぎないか）  — 対象 78 件中、確かめられないのは 3 件
  OK  在庫 契約が求める data-testid が画面に出ている  — 36 画面で目印を確認
  OK  在庫 画面でもAPIでもないルートが応答する（CSV配信・死活・外部照会）  — 3/3 件が応答
  OK  見た目 共通CSS(/ui.css)を配っていて、全画面が読んでいる  — 34 画面すべてが読んでいる
  OK  見た目 指示したクラスが実際に画面に付いている  — 34 画面で 3 クラスすべてを確認
  OK  契約 押しても何も起きないボタンにしない（灰色3つが理由へ繋がる）  — 3 個: reception_done_all_delete, reception_done_delete, temp_save
  OK  書き込み 作って・確かめて・取り消せる（GETだけでは分からない）  — 作成→取消の往復ができた（id=62）
  OK  データ 実装が読んでいる中身が data/ と一致する（数字だけ見ても分からない）  — 513 項目が data/ と一致
  OK  見た目 トップの見出しと共通ナビが契約どおり（5実装で同じに見えるか）  — 34 画面で見出しとナビを確認
  OK  見た目 トップの本文が契約どおり（ナビの複製を並べない）  — 説明あり・本文のリンク 4 本
  NG  契約 本日の患者に受付区分のタブがある  — 既定表示が 0 行。契約は「省略時はマスタの1つ目」なので 1 行（初診）が正しい
  OK  見た目 画面名がナビの表示と同じ（同じ画面を別の名前で呼ばない）  — 9 画面の名前がナビと一致
  OK  見た目 ナビに無い画面の名前も契約と同じ（患者名を見出しに混ぜない）  — 13 画面の名前が契約と一致

29 件中 1 件 失敗
```

### FastAPI — 29件中1件失敗

```
  OK  GET /healthz が 200 で {"status":"ok"} を返す
  OK  起動していて、応答が返る  — 19ms
  OK  検算1 売上が分類別・担当別・日別・総合計で一致する  — 4値とも 5,189,585 円
  OK  検算1 分類別の構成比の和がちょうど100.0%  — 和=100.0
  OK  検算2 単価未設定の行を0円で合計に入れず、未算入の行数を出す  — 伝票28 税抜49,528 未算入1行
  OK  検算2 消費税は伝票単位で1回だけ切り捨て  — 税2,753 税込30,285
  OK  検算2 この規則を、いまのデータで確かめられているか（検算そのものの点検）  — 150枚中 132枚 が丸め方を区別する（見張りとして働いている）
  OK  検算3 体温が全患者で同じ値になっていない  — 14 種 / 31 件
  OK  検算4 カルテの画面と印刷で同じ値が出る  — 20 組を比べて差なし
  OK  検算5 基準の外にある値は判定欄と色の両方に出る  — 50 項目の判定が一致
  OK  検算6 予約が担当・処置室のどちらでも重ならない  — 50 件で重なり0
  OK  検算7 入院の記録行に実施者が必ず入っている  — 108 件中 実施者なし 0 件
  OK  検算9 削除済みは一覧から消えるが件数には残る  — 一覧から消えても集計に残る
  OK  検算8 画面から辿れるリンクが全部生きている  — 60 画面を辿って切れなし
  OK  在庫 契約の画面ルートが全部ある（未実装はクローラーに見えない）  — 40/42 件ある（2 件は確かめられない: /papers/{paper_id}, /papers/{paper_id}/remove）
  OK  在庫 契約のAPIルートが全部ある  — 35/36 件ある（1 件は確かめられない: /api/papers/{paper_id}）
  OK  在庫 この検査自体が働いているか（確かめられない分が多すぎないか）  — 対象 78 件中、確かめられないのは 3 件
  OK  在庫 契約が求める data-testid が画面に出ている  — 36 画面で目印を確認
  OK  在庫 画面でもAPIでもないルートが応答する（CSV配信・死活・外部照会）  — 3/3 件が応答
  OK  見た目 共通CSS(/ui.css)を配っていて、全画面が読んでいる  — 36 画面すべてが読んでいる
  OK  見た目 指示したクラスが実際に画面に付いている  — 36 画面で 3 クラスすべてを確認
  OK  契約 押しても何も起きないボタンにしない（灰色3つが理由へ繋がる）  — 3 個: complete_delete_all, complete_delete_one, temp_save
  OK  書き込み 作って・確かめて・取り消せる（GETだけでは分からない）  — 作成→取消の往復ができた（id=61）
  OK  データ 実装が読んでいる中身が data/ と一致する（数字だけ見ても分からない）  — 513 項目が data/ と一致
  OK  見た目 トップの見出しと共通ナビが契約どおり（5実装で同じに見えるか）  — 34 画面で見出しとナビを確認
  OK  見た目 トップの本文が契約どおり（ナビの複製を並べない）  — 説明あり・本文のリンク 4 本
  NG  契約 本日の患者に受付区分のタブがある  — 既定表示が 0 行。契約は「省略時はマスタの1つ目」なので 1 行（初診）が正しい
  OK  見た目 画面名がナビの表示と同じ（同じ画面を別の名前で呼ばない）  — 9 画面の名前がナビと一致
  OK  見た目 ナビに無い画面の名前も契約と同じ（患者名を見出しに混ぜない）  — 13 画面の名前が契約と一致

29 件中 1 件 失敗
```

### Next.js — 29件中1件失敗

```
  OK  GET /healthz が 200 で {"status":"ok"} を返す
  OK  起動していて、応答が返る  — 26ms
  OK  検算1 売上が分類別・担当別・日別・総合計で一致する  — 4値とも 5,189,585 円
  OK  検算1 分類別の構成比の和がちょうど100.0%  — 和=100.0
  OK  検算2 単価未設定の行を0円で合計に入れず、未算入の行数を出す  — 伝票28 税抜49,528 未算入1行
  OK  検算2 消費税は伝票単位で1回だけ切り捨て  — 税2,753 税込30,285
  OK  検算2 この規則を、いまのデータで確かめられているか（検算そのものの点検）  — 150枚中 132枚 が丸め方を区別する（見張りとして働いている）
  OK  検算3 体温が全患者で同じ値になっていない  — 9 種 / 10 件
  OK  検算4 カルテの画面と印刷で同じ値が出る  — 16 組を比べて差なし
  OK  検算5 基準の外にある値は判定欄と色の両方に出る  — 50 項目の判定が一致
  OK  検算6 予約が担当・処置室のどちらでも重ならない  — 60 件で重なり0
  OK  検算7 入院の記録行に実施者が必ず入っている  — 108 件中 実施者なし 0 件
  OK  検算9 削除済みは一覧から消えるが件数には残る  — 一覧から消えても集計に残る
  OK  検算8 画面から辿れるリンクが全部生きている  — 60 画面を辿って切れなし
  OK  在庫 契約の画面ルートが全部ある（未実装はクローラーに見えない）  — 40/42 件ある（2 件は確かめられない: /papers/{paper_id}, /papers/{paper_id}/remove）
  OK  在庫 契約のAPIルートが全部ある  — 35/36 件ある（1 件は確かめられない: /api/papers/{paper_id}）
  OK  在庫 この検査自体が働いているか（確かめられない分が多すぎないか）  — 対象 78 件中、確かめられないのは 3 件
  OK  在庫 契約が求める data-testid が画面に出ている  — 37 画面で目印を確認
  OK  在庫 画面でもAPIでもないルートが応答する（CSV配信・死活・外部照会）  — 3/3 件が応答
  OK  見た目 共通CSS(/ui.css)を配っていて、全画面が読んでいる  — 37 画面すべてが読んでいる
  OK  見た目 指示したクラスが実際に画面に付いている  — 37 画面で 3 クラスすべてを確認
  OK  契約 押しても何も起きないボタンにしない（灰色3つが理由へ繋がる）  — 3 個: karte-temp-save, reception-complete-delete, reception-complete-delete-all
  OK  書き込み 作って・確かめて・取り消せる（GETだけでは分からない）  — 作成→取消の往復ができた（id=61）
  OK  データ 実装が読んでいる中身が data/ と一致する（数字だけ見ても分からない）  — 513 項目が data/ と一致
  OK  見た目 トップの見出しと共通ナビが契約どおり（5実装で同じに見えるか）  — 35 画面で見出しとナビを確認
  OK  見た目 トップの本文が契約どおり（ナビの複製を並べない）  — 説明あり・本文のリンク 5 本
  NG  契約 本日の患者に受付区分のタブがある  — 既定表示が 25 行。契約は「省略時はマスタの1つ目」なので 1 行（初診）が正しい
  OK  見た目 画面名がナビの表示と同じ（同じ画面を別の名前で呼ばない）  — 9 画面の名前がナビと一致
  OK  見た目 ナビに無い画面の名前も契約と同じ（患者名を見出しに混ぜない）  — 14 画面の名前が契約と一致

29 件中 1 件 失敗
```

## 入れ直しの問題ではなく、実装の問題として報告するもの（3件・未修正）

**投入手順は同じデータを5実装とも正しく入れられている**（検算1・2は5実装とも一致）。
この3件は「本日の受付が0件」で今まで隠れていた、実装側の既存バグと見られる。
**指示どおり、コードは直していない。**

1. **Rails（8414）— 既定表示が0行**。
   `db/schema.rb` の `receptions` テーブルに `kind` 列があり、`db/seeds.rb` も
   `Reception.create!(..., kind: r["kind"], ...)` としているが、
   **`data/seed.json` の receptions に `"kind"` というキーは存在しない**
   （実際に持つキーは `id, patient_id, display_no, received_at, owner_purpose,
   medical_purpose, status, staff_id` のみ）。そのため全25行の `kind` が常にnilになり、
   `app/models/reception.rb` の `scope :of_kind` が機能しない。
   `spec/model.md` のReception項目一覧にも `kind` は無く、`tests/inventory.py` の
   `_kind_tabs` 検査は `medical_purpose` を `masters.json` の `reception_kinds[].name`
   と突き合わせて期待値を作っている（`owner_purpose`/`kind`ではない）。
   Railsの`kind`列自体が契約に無い設計になっている可能性がある。

2. **FastAPI（8415）— 既定表示が0行**。Railsと同じ症状（0行）だが、
   コードは未確認（レーンDへの確認が必要）。

3. **Next.js（8405）— 既定表示が25行（絞り込みなし）**。
   `kind` パラメータ省略時に「マスタの1つ目」へフォールバックする処理が
   無いか効いていない可能性。コードは未確認（レーンEへの確認が必要）。

## 守ったこと

- `stacks/` 以外は変更していない。`data/` `spec/` `tests/` は読むだけで書き換えていない
- コミット・pushはしていない
- 実装のコードは一切直していない（上記3件は発見のみ）
- 25分後の自動確認などのフォールバックは入れていない

**待機する。次の指示があれば対応する。**
