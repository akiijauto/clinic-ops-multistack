# 指揮役の裁定（契約に関するもの）

**ここに書いたことは決定済み。** レーンは蒸し返さず、これに従うこと。
契約の文書（`spec/`）と食い違いを見つけたら、**この文書が正**として指揮役へ報告する。

## 2026-09-05 契約の凍結にあたって

`spec/acceptance.md` の作成者から6件の判断が報告された。**すべて採用する。**
理由を添えて記録しておく（後から「なぜこう決めたか」を追えるようにするため）。

| # | 論点 | 裁定 | 理由 |
| --- | --- | --- | --- |
| 1 | 売上の「担当」軸 | **`Billing.staff_id`（診療担当）**を使う。`cashier_staff_id`（会計担当）は使わない | 業務上「誰が売上を立てたか」は診療した人。会計を打った人ではない |
| 2 | 売上の「分類」軸 | 料金の2階層のうち**上位1階層のみ** | 階層の深さは比較の題材にならない（`model.md` で4階層→2階層に減らした判断と同じ筋） |
| 3 | **消費税の扱い** | **伝票単位で1回だけ切り捨て。** 分類別・担当別の内訳には消費税を含めず、**税抜で3方向一致**を取る | 内訳へ税を按分すると、**按分の方法しだいで答えが変わり、検算にならない**。税抜なら按分が要らず、5実装で必ず一致する |
| 4 | **HTMLの目印** | **両方残す。役割で分ける**（下記） | 一度「`data-check` に統一」と裁定したが、**それも撤回した。2つは別の仕事をしている** |
| 5 | 入院の実施者 | フィールド名は **`performed_by_staff_id`** | `model.md` に記載が無かった。確定して `model.md` へ反映済み |
| 6 | 予約の重なり判定 | **半開区間**（終了時刻＝次の開始時刻は重ならない扱い） | そうしないと、連続した枠が全部「重複」と判定される |

### 3番について補足

**税の計算順序は、決めないと必ず5実装で割れる。** 1円のずれが出て、
「どちらが正しいか」を後から議論することになる。

題材の運用ルールにも「金額の計算順序は想定でも1つに決めて明記する」とある。
ここでは **伝票単位・1回だけ・切り捨て** で固定する。

### 4番について補足（裁定を2度変えたので、経緯を残す）

**指揮役は同じ論点で2回裁定を変えた。** 混乱を避けるため、いまの結論と理由を書いておく。

| 目印 | 答える問い | 例 |
| --- | --- | --- |
| **`data-testid`** | **その要素があるか**（存在） | `screen-today` `row-reception` `error-banner` `empty-reception` |
| **`data-check`** | **その値が正しいか**（値） | `billing.net_amount` `progress_note.temperature_c` |

**両方使う。** 片方に寄せない。

経緯:

1. 最初、指揮役は `openapi.yaml` の担当へ「`data-testid` を定義せよ」と指示した
2. 並行して `acceptance.md` の担当が `data-check` を新設した。**衝突に見えた**ので、
   指揮役は「`data-check` に統一。`data-testid` は使わない」と裁定した
3. その後 `openapi.yaml` の設計を実際に読んで、**衝突ではなかった**ことが分かった。
   `data-testid` は要素の存在、`data-check` は値の正しさで、**別の関心**である。
   片方へ寄せると2つを混ぜることになるので、**2番目の裁定を撤回した**

**教訓**: 裁定する前に、両方の設計を読む。**「名前が似ている」は「同じもの」ではない。**
指揮役は名前だけを見て衝突と判断し、実物を読まずに裁定した。

## 2026-09-05 夜 — 契約の欠陥1件を修正（レーンAの指摘 Q-A-08）

**`spec/openapi.yaml` と judge（`tests/checks.py`）が別の項目名を使っていた。**
そのままだと「契約どおり作ったのに落ちる」実装が5つできる。

### どちらが正かを実測で決めた

| 出どころ | 税抜合計 | 未算入件数 | 税込合計 |
| --- | --- | --- | --- |
| `spec/acceptance.md` の `data-check` キー | `net_amount` | `excluded_count` | `total_amount` |
| `tests/checks.py`（judge） | `net_amount` | `excluded_count` | `total_amount` |
| `spec/openapi.yaml` | taxable+nontaxable | excluded_detail_count | total |

**2対1で openapi がずれている。** よって openapi を直した。

### 売上集計は形そのものを変えた

`group_by` で1軸ずつ返す作りだったが、**検算1は3軸を同時に突き合わせる**もので、
この形では成立しない。別々に取ると取った時点がずれる余地も残る。

```
by_category / by_staff / by_date / total_net_amount   ← 同時に返す
```

`from` / `to` は**省略可**にした（judge はパラメータ無しで呼ぶ）。

### 教訓

契約を複数人で分けて書くと、**同じものに別の名前が付く**。
「名前の一覧」を先に決めてから書けば防げた。
`acceptance.md` の `data-check` キー表が実質それになっていたが、
**openapi の担当がそこを見ていなかった。**

## 2026-09-05 夜 — 検算そのものが検算になっていなかった（レーンCの指摘）

レーンC（Laravel）が money を通したとき、こう報告してきた。

> `data/seed.json` の `billing_details[].quantity` は全件整数だったので、
> 「明細ごとに丸めるか合計後に1回で丸めるか」の差は今回のデータでは顕在化しませんでした

**調べたら、そのとおりだった。**

| 調べたこと | 結果 |
| --- | --- |
| 明細600行のうち、数量が非整数のもの | **0行** |
| 伝票150枚のうち、明細ごと丸め ≠ 伝票単位丸め になるもの | **0枚** |
| 単価112件のうち、10で割り切れないもの | 3件（11 / 22 / 33） |

端数の出る単価は**存在するが、1枚の伝票の中で端数が1円以上に積み上がる組み合わせが無い**。

### つまり

**「消費税は伝票単位で1回だけ切り捨て」という規則は、どちらの実装でも緑になる。**
検算2はこの規則について**見張りとして働いていない**。

`PLAN.md` の「緑と呼んでよい条件」に **「見張りを足したら、わざと壊して落ちることを確かめる」**
と書いてあるが、**この検算はそれを通っていなかった**。

### どうしたか

**データを作り直さなかった。** 作り直すと総合計（5,185,704円）が変わり、
既に到達している3実装の検証をやり直すことになる。費用に見合わない。

代わりに**検算そのものを点検する項目を judge に足した**。

```
OK  検算2 この規則を、いまのデータで確かめられているか（検算そのものの点検）
    — ★ 150枚すべてで丸め方の差が出ない。この規則は**いまのデータでは検証できていない**
```

合否ではなく**事実として出す**（常に通す）形にした。赤にすると実装の失敗と紛れるため。

### 教訓

**「検算が緑」と「検算が働いている」は別のこと。**

このプロジェクトは検算9項目を売りにしている。1つでも「何も見ていない」ものが混ざったまま
公開すると、**主張そのものが嘘になる**。

検算を書いたら、**その検算が落ちる入力が実際に存在するか**を確かめること。
存在しないなら、それは検算ではなく飾りである。

## 2026-09-05 夜 — judge 自身にバグがあった（レーンAの指摘 Q-A-10）

**実装ではなく、判定側が間違っていた。**

`tests/checks.py` の `_data_check` は、正規表現で `data-check` 属性を持つ要素を拾っていた。

```python
r"<([a-zA-Z][\w-]*)\b([^>]*?)>(.*?)</\1>"
```

これを `<!DOCTYPE html><html>…</html>` という**普通の文書**に当てると、
拾えるのは **`html` ただ1つ**になる。

`re.finditer` は最も左から始まる一致を返す。`</html>` が文書に1つしか無いので、
`<html>` を起点にした `.*?` が**残り全部を飲み込んで**成立してしまい、
中に入れ子になった `data-check` は一切見つからない。

### 何が起きるところだったか

- **HTMLの書き方の問題ではない。** どの言語・どのスタックで作っても同じように落ちる
- レーンAの実装は**正しく値を出していた**（本人が curl で確認済み）
- screen 組に着手した最初のレーンだったので、**まだ誰の結果にも表面化していなかった**

もし気づかずに進んでいたら、**5実装すべてが「data-check が付いていない」と言われ続け、
5人が自分の実装を疑って直そうとする**ところだった。

### どう直したか

**正規表現をやめ、`html.parser` で書き直した。** 入れ子も正しく扱う。

```
普通の文書   → ['1', '2']      （以前は []）
入れ子あり   → ['3,500']       （<b>3</b>,500 を1つの値として読む）
属性が後ろ   → ['7']
目印なし     → []
```

直後にレーンAの実装で測り直したら、**screen 3件が緑になった**。実装は最初から正しかった。

### 教訓

**判定する側が間違っていると、正しい実装が落ちる。** しかも落ちた側は
自分を疑うので、間違いが見つかるまで時間が溶ける。

`PLAN.md` に「見張りを足したら、わざと壊して落ちることを確かめる」と書いたが、
**「見張りが正しいものを通すか」も確かめる必要がある**。落ちることだけ確かめても足りない。

レーンAは「止まらず進めるべき点は無いか検討したが、tests 側の修正を待つほかない」と
判断して報告してきた。**これは正しい。** `PROTOCOL.md` の「止まらない質問は仮決めして進む」は、
**仮決めで進める場合の話**であり、判定側が壊れているときは進めない。

## 2026-09-06 — judge のバグ2件目（レーンDの指摘）

**HTTPのヘッダ名は大文字小文字を区別しない**（RFC 9110）のに、judge は区別して読んでいた。

```python
headers.get("Content-Type", "")   # ← これ
```

実測すると、実装によって返し方が違う。

| 実装 | 返すキー |
| --- | --- |
| Go | `Content-Type` |
| Laravel | `Content-Type` |
| **FastAPI / Starlette** | **`content-type`** |

FastAPI では `get("Content-Type")` が常に `None` になり、
`_dead_links` の「HTMLでなければ辿らない」判定が**全ページで真**になって、
**トップの1画面しか辿れなかった**。

### 何が起きるところだったか

レーンAは Go で、Go は慣習的に `Content-Type` を返す。**だから表面化しなかった。**
FastAPI のレーンDが最初に当たった。

もし気づかずに進んでいたら、**FastAPI と Next.js だけが検算8を通せない**という
「実装の質の差」に見える現象が残るところだった。**実際は判定側の欠陥**である。

### どう直したか

`Client` の側でヘッダのキーを**小文字へ正規化**した。判定する側が
**実装の書き癖に左右されてはいけない**ので、入口で揃える。

直後の実測: FastAPI が **60画面**を辿って切れなし（以前は1画面）。Go は回帰なし。

### 教訓

**judge のバグは、実装の差として現れる。**

今回も前回（`_data_check` の正規表現）も、**特定のスタックだけが落ちる**形で出た。
「あの実装だけ通らない」を見たら、**まず判定側を疑う**こと。

そして今回も、レーンDは「まず自分の側を確かめてから」の順序を守り、
curl でリンクが出ていること・`Client.get` のヘッダが小文字であることを
**実測してから**報告してきた。

## 2026-09-06 — judge の欠陥3件目（レーンCの発見から）

レーンCは検算8で「1画面しか辿れない」に当たり、**自分のミスとして**報告してきた。

> 共通レイアウトのナビ等を `url('/xxx')` で書いていた。Laravelの `url()` は絶対URL
> （`http://127.0.0.1:8403/xxx`）を返すため、共通クローラーの `href="/..."` 判定に
> 掛からず、リンクとして1つも拾われていなかった

**が、これは判定側の問題でもある。**

絶対URLは正しいHTMLであり、ブラウザは普通に辿る。拾えないままだと、
**判定側が実装に「ルート相対で書け」と強制する**ことになる。

`spec/README.md` は「**画面の見た目は各スタックの流儀でよい。揃えるのは振る舞いだけ**」と
定めている。**書き方の強制はこれに反する。**

### どう直したか

巡回器が**絶対URLも辿る**ようにした。ただし**同じ相手のものだけ**で、外のサイトへは出ない。

直後の実測: Go 14画面 / Laravel 60画面 / FastAPI 60画面。回帰なし。

### 途中で自分が壊した

定義（`HREF_RE`）を入れ忘れたまま使用箇所だけ差し替え、3実装とも
`NameError` で落とした。**judge が「検査自体が落ちた」と報告した**ので、
すぐ原因が分かった。検査の中で例外を握って合否に混ぜず、
**壊れたことを壊れたと出す**作りにしておいてよかった。

### 判定側の欠陥はこれで3件目

| # | 何が | どう現れたか |
| --- | --- | --- |
| 1 | 正規表現が普通のHTML文書を読めない | 全実装で `data-check` が0件 |
| 2 | HTTPヘッダを大文字小文字で区別 | **FastAPI だけ**1画面しか辿れない |
| 3 | 絶対URLを辿らない | **Laravel だけ**1画面しか辿れない |

**3件とも「特定の実装だけが落ちる」形で出た。** 実装の質の差に見えるが、
中身は判定側の欠陥である。

**「あの実装だけ通らない」を見たら、まず判定側を疑うこと。**

## 2026-09-06 — 第2段階の完了。5実装すべてが全14件を通した

指揮役が `tests/compare.py` で横並びに測った結果:

```
        A(Go)  B(Rails)  C(Laravel)  D(FastAPI)  E(Next.js)
14件      OK      OK        OK          OK          OK

測れた範囲で、実装ごとの食い違いは無い。
```

**言語もフレームワークも違う5実装が、同じ契約・同じデータ・同じ判定に対して
完全に同じ答えを返した。** これがこの企画の中身である。

### レーンDによる訂正（孤児プロセスの件）

私が `PORTS.md` に「握ったまま死んだソケット」と書いたのは**誤りだった**。
レーンDが追い直して正体を突き止めた。

`Stop-Process` で止めていたのは WatchFiles の reloader（親）だけで、
**multiprocessing で fork した worker（子）が生き残ってソケットを掴み続けていた**。

`Get-NetTCPConnection` の示すPIDが引けなかったのは、**それが既に死んだ親のPIDだったから**。

**「プロセスが無いのにポートが埋まっている」と見えたら、孤児を疑う。**

私は「Windowsで稀に起きる現象」という説明で止めてしまった。
**説明がついた気になると、そこで調べるのをやめる。** レーンDは止めずに追った。


---

## R-20 記録が0件のとき、APIは404でも500でもなく200で空を返す（2026-09-06）

**発端**: レーンAが「投薬APIは記録0件の年度で404を返していたが、画面側は0件でも200で
空のマス目を描いていた。非対称なので200に揃えた」と報告した（`qa/lane-a.md` Q-A-16）。
これは1実装の話に見えて、**5実装が揃えるべき契約の問題**である。

**実測**（2026-09-06 10:05、記録0件の患者 karte_no=10018）

| 実装 | `/api/patients/10018/dosing/heartworm` | `/api/.../prevention/heartworm` |
| --- | --- | --- |
| Go | **200** `{fiscal_year,id,kind,m01…}` | **200** `{items,total}` |
| Next.js | **200** 同上 | **200** 同上 |
| Rails | **500** | **500** |
| Laravel | **500** | **500** |
| FastAPI | 404（未実装） | 404（未実装） |

**5実装が3通りに割れている。**

### 裁定

**200 で空を返す。** 理由は3つ。

1. **404 は「その資源が無い」の意味である。** 患者は在るし、種別も在る。
   無いのは「その組み合わせの記録」であって、資源そのものではない。
   これを404にすると、**存在しない患者を指したときと区別がつかない**
2. **画面側が0件で200を返している。** 同じ問いに画面とAPIで別の答えを返すのは、
   検算4（画面と印刷で同じ値）と同じ型の食い違いである
3. **500は論外。** 記録が無いのは異常事態ではない。**通常の入力で500を返す実装は壊れている**

404を返してよいのは、**患者そのものが存在しない**とき、または
**種別が語彙に無い**ときだけとする。

### 各レーンへ

- **レーンB（Rails）・レーンC（Laravel）**: 500になっている。**直すこと。** 例外が
  そのまま外に出ている疑いが強い
- **レーンD（FastAPI）**: これから作るぶんは最初から200で空を返すこと
- **レーンA（Go）・レーンE（Next.js）**: 現状のままでよい

### 指揮役の反省 — 在庫検査の甘さ（判定器の欠陥5件目）

**在庫検査は「404か501か接続不可でなければ在る」と判定していた。**
だから **500 を「在る」と数えていた。** Rails と Laravel はこの検査を緑で通っている。

「作っていないほど緑になる」を塞いだつもりで、**「壊れていても緑になる」を作った。**
在庫検査を、5xx も「無い」側に数えるよう直す。

    在るかどうかを見る検査でも、**どう壊れているかを見ないと嘘になる。**


---

## R-21 「書類（紙カルテPDF）」は範囲内。`model.md` の「落としたもの」から外す（2026-09-06）

**発端**: レーンDが `qa/lane-d.md` D-11 で、契約どうしの矛盾を止まらずに記録した。

- `spec/model.md`「落としたもの」: **`KartePdf`（紙カルテの取込）— ファイルの取り扱いが主題になってしまう**
- `spec/screens.md` 13番: **書類（紙カルテPDF）** として、取込・取消・「元から無い」印まで詳細に規定
- `spec/openapi.yaml`: **6ルート**が実在（`/animals/{karte_no}/papers` `/papers/{paper_id}`
  `/papers/{paper_id}/remove` `/papers/no-paper` `/api/patients/{karte_no}/papers` `/api/papers/{paper_id}`）

**同じ契約の中で、片方が「作らない」、もう片方が「これを作る」と言っている。**

### 裁定

**書類は範囲内とする。** `model.md` の「落としたもの」から `KartePdf` を外す。

理由:

1. **具体的な側が勝つ。** `screens.md` と `openapi.yaml` は満たすべきことまで書いてある。
   `model.md` の1行は「落とした」としか書いていない
2. **5実装すべてが既に作っている。** いま「落とす」と裁定すると、動いているものを消すことになる
3. 落とした理由「ファイルの取り扱いが主題になってしまう」は**回避できている**。
   実装はどれもPDFの実体を持たず、**取込の記録（メタデータ）だけ**を扱っている

### ただし、書いてあるのに確かめていないことが1つある

`screens.md` 13番に **「PDF以外の形式のファイルは取り込みを拒否する」** とあるが、
**これを確かめる検算は無い。** 実体を持たない実装で、この条件がどう満たされているかは未確認。

**「書いてあるが確かめていない」ものを、黙って通さない。** 既知の穴として記録する。
塞ぐなら検算を足す。足さないなら `screens.md` から落とす。どちらかにする。

---

## R-22 `/ward` の `data-testid` は契約どおり `screen-ward-day`。実装が出していないだけ（2026-09-06）

**発端**: レーンDが「`/ward` の x-data-testid（`screen-ward-day` 期待）と実装（`screen-ward`）が
食い違う」と記録した。**実測したら、そうではなかった。**

| 実装 | `/ward` | `/ward/day` |
| --- | --- | --- |
| Go | `screen-ward-day` | `screen-ward-day` |
| Rails | `screen-ward-day` | `screen-ward-day` |
| Laravel | `screen-ward-day` | `screen-ward-day` |
| Next.js | `screen-ward-day` | `screen-ward-day` |
| **FastAPI** | **なし** | **なし** |

**契約と実装の食い違いではなく、FastAPI だけ `data-testid` を1つも出していない。**
4実装は契約どおりに出している。契約は変えない。**FastAPI が合わせる。**

### 指揮役の反省 — 目印を確かめる検査が無かった（判定器の穴6件目）

`spec/README.md` で目印を2系統に分けると決めていた。

- `data-testid` = **その要素が在るか**
- `data-check` = **その値が正しいか**

`data-check` は検算で使っていたが、**`data-testid` を確かめる検査は1つも書いていなかった。**
だから FastAPI が1つも出していないことに、誰も気づかなかった。

**契約に書いた目印を、契約が要求したとおりに確かめていなかった。**
`tests/inventory.py` に、`openapi.yaml` の `x-data-testids` を1件ずつ照合する検査を足す。


---

## R-23 契約とデータが食い違っている — `kind_id` は integer なのに、データは文字列コードしか持たない（2026-09-06）

**3レーンが独立に同じ穴を踏み、独立に同じ解決に到達した。**

| レーン | 症状 | 解決 |
| --- | --- | --- |
| A（Go） | `strconv.Atoi` 一本槍で数値以外は即404 | `resolveKind`（数値idを先に試し、ダメならコードで引く） |
| C（Laravel） | `int $kindId` 型ヒントに文字列が来て **TypeError → 500** | `FixedData::preventionKind(string)` で両対応 |
| E（Next.js） | 数値位置しか受け付けず404 | `masters.ts` に `resolveKindParam()` |

**同じ間違いを3つの言語で3回作ったのなら、間違えたのは実装ではなく契約である。**

### 食い違いの中身

- `spec/openapi.yaml`: `kind_id` を **integer** と型付け
- `data/seed.json`: `dosings[].kind` / `preventions[].kind` は
  **`"heartworm"` のような文字列コード**。数値idは1つも無い

判定器が「値を data/ から引く」方式に変わったことで、契約の型と実データの型の
食い違いが**初めて表に出た**。それまでは固定値 `1` を渡していたので、
**契約どおりに実装したほうが落ちる**という逆転が隠れていた。

### 裁定

**いまは実装側の両対応（数値・文字列のどちらでも引ける）を正とする。** 契約は直さない。

理由は、5実装のうち4つが既に両対応になっており、いま契約を `string` に直すと
**数値idで叩いている既存の呼び出しを壊す**恐れがあるため。後方互換を保ったまま
両方受けるのが、いちばん壊さない。

### ただし、これは先送りである

**正しくは、契約とデータのどちらかが間違っている。** どちらを直すかを決めていない。

- `openapi.yaml` の `kind_id` を string にする（データに合わせる）
- `data/seed.json` に数値idを持たせる（契約に合わせる）

**「両方受ける」は、どちらが正かを決めない言い訳にもなる。**
決めるまでは既知の穴として残す。黙って通さない。


---

## R-23改 撤回して差し替える — 間違えていたのは実装ではなく**判定器**だった（2026-09-06）

**R-23 の裁定は誤りだった。撤回する。**

R-23 ではこう書いた。

> 同じ間違いを3つの言語で3回作ったのなら、間違えたのは実装ではなく契約である。

**この推論が間違っていた。** 3レーンが同じ対処をしたのは事実だが、そこから
「契約が悪い」へ飛んだ。**実際に間違っていたのは、その手前にある判定器だった。**

### 何が起きていたか

- `spec/openapi.yaml`: `kind_id` は **integer**
- `data/seed.json`: `dosings[].kind` は **`"heartworm"` という文字列コード**
- `data/masters.json`: `prevention_kinds` は**順序つきの一覧**。`heartworm` は**3番目**

つまり**整数のidは導ける**。`masters.json` の並び順がそれである。
ところが判定器は `dosings[0]["kind"]` の文字列をそのままパスに埋めていた。

**契約どおり整数として受けている実装（FastAPI）だけが422を返し、
判定器はそれを「無い」と数えた。**

    判定器が契約と違う型で叩けば、**契約を守った実装が落ちる。**
    「4対1で落ちているほうが悪い」は成り立たない。

### 誰が見つけたか

**レーンD（FastAPI）が、指揮役の裁定に反論して見つけた。**

> `tests/inventory.py` の `_samples()` が…`kind_id="heartworm"` という文字列を
> パスパラメータに渡している。契約は `type: integer` と定義しており、
> この実装は契約どおり整数として受けているので、文字列を渡すと422になるのは
> Pydantic側の正しい型検証だと考えている。

**指揮役が「4実装が通っているのだから、あなたが直せ」と2度伝えたあとの反論である。**
反論しなければ、契約を守った実装のほうが「壊れている」ことにされていた。

### 差し替えた裁定

1. **判定器が契約どおり整数を渡す。** `masters.json` の並び順（1始まり）から引く。修正済み
2. **契約は変えない。** `kind_id` は integer のまま
3. 3レーンが入れた「数値・文字列の両対応」は**残してよい**。契約が許す範囲を
   広げているだけで、契約に反していない。ただし**それが正しさの根拠ではない**
4. `data/seed.json` が数値idを持たない点は**残った課題**として記録する。
   コードから並び順を引く形は、**並び順が変わると意味が変わる**ので脆い

### 指揮役の反省（同日4件目）

| # | 内容 | 見つけたのは |
| --- | --- | --- |
| 1 | 「テストが緑」を「完了」と読んだ | オーナー |
| 2 | 実データを見ずに判定器へ固定値を書いた | レーンA |
| 3 | 作業後の数字を作業前と比べ、成果を読み違えた | レーンCの報告 |
| 4 | **判定器が契約と違う型で叩き、契約を守った実装を「壊れている」と断じた** | **レーンDの反論** |

4件とも指揮役の判断の誤りで、4件とも**指揮役以外が指摘して初めて分かった。**

**多数決を根拠にしたのが今回の新しい失敗である。**
5実装のうち4つが通っていることは、**その4つが正しいことの証明ではない。**
判定器が間違っていれば、間違いに合わせた実装のほうが通る。


---

## R-20改 「404は論外」は言い過ぎだった。契約は404を認めている（2026-09-06）

R-20 でこう書いた。

> **500は論外。** 記録が無いのは異常事態ではない。通常の入力で500を返す実装は壊れている
> 404を返してよいのは、患者そのものが存在しないとき、または種別が語彙に無いときだけとする。

**後半が誤りだった。** 契約を読み直したら、こう書いてある。

```
/api/patients/{karte_no}/dosing/{kind_id}:
  responses:
    "200": OK
    "404": NotFound      ← 契約が正式な応答として定義している
/animals/{karte_no}/dosing/{kind_id}:
  responses:
    "200": HtmlOk
    "404": NotFound      ← 同上
```

**契約は「記録が無いとき404」を認めている。** 私は契約を読まずに「404を返してよいのは
患者が存在しないときだけ」と裁定した。**契約より自分の考えを上に置いた。**

### 誰が見つけたか

**またレーンD（FastAPI）である。** 同じ相手の反論で、同じ日に2回訂正した。

> `openapi.yaml` はこのGETに404を正式なレスポンスとして定義している（「記録がまだ無い」を
> 表す404）ため、実装の欠けではなく **karte_no と kind_id もペアで選ぶ必要がある**ことによる
> 誤検知だと考えている。

指摘のとおりだった。判定器は `karte_no=10018` と `kind_id=3` を**別々に**選んでいた。
この患者に heartworm の投薬記録は無い。**契約どおり404が返り、それを「欠け」と数えていた。**

    契約が404を認めている経路に、記録の無い相手を選んで投げれば、**正しい実装ほど落ちる。**

これは `visit_id` のときとまったく同じ誤りである（レーンAが発見）。**同じ型の誤りを2回した。**
判定器はいま「**診察と投薬の両方を持つ患者**」を選び、その患者の値だけを使う。

### 差し替えた裁定

| 状態 | 判断 |
| --- | --- |
| **500** | **契約に無い。壊れている。直す**（ここは R-20 のまま変わらない） |
| **404** | **契約が認めている。** 記録が無いときに返してよい |
| **200 で空** | **契約が認めている。** こちらも正しい |

**契約が両方を許しているので、5実装が違う答えを返しても契約違反ではない。**
ただしこの企画の狙いは「同じ契約から同じものが出ること」なので、
**揃えるために200で空を選ぶ**——これは契約の要求ではなく、この企画の決めごとである。
現に5実装とも200を返すようになっている。

**「契約が要求している」と「この企画で決めた」を混ぜて書いたのが、R-20 の書き方の誤りだった。**

### 指揮役の誤り（同日5件目）

| # | 内容 | 見つけたのは |
| --- | --- | --- |
| 1 | 「テストが緑」を「完了」と読んだ | オーナー |
| 2 | 実データを見ずに判定器へ固定値を書いた | レーンA |
| 3 | 作業後の数字を作業前と比べ、成果を読み違えた | レーンCの報告 |
| 4 | 判定器が契約と違う型で叩き、契約を守った実装を「壊れている」と断じた | レーンDの反論 |
| 5 | **契約を読まずに裁定を書き、契約が認めている応答を「論外」と断じた** | **レーンDの反論** |

**5件のうち2件が、同じ相手からの反論で分かった。**
反論を受け付ける関係が無ければ、指揮役の誤りはそのまま仕様になる。


---

## R-24 検査画面の既定表示は「最新の保存済み検査」。空の新規フォームではない（2026-09-06）

**発端**: レーンA（Go）が、指示されたクラスが出ない原因を調べる中で見つけた。

> `GET /animals/{karte_no}/exam`（クエリ無し）の既定表示が常に空欄の新規フォームだった。
> その結果「**基準外の値を示す判定・色が既定表示には未来永劫出ない**」設計になっていた。

`spec/openapi.yaml` はこの経路に**クエリパラメータを1つも定義していない**。
つまり既定で何を出すかは書かれておらず、実装の裁量になっていた。**契約の抜けである。**

### 実測（2026-09-06 11:00、`/animals/10003/exam` の既定表示）

| 実装 | `out-of-range` の出現 |
| --- | ---: |
| Go（修正前） | **0**（空フォーム） |
| Rails | 10 |
| Laravel | 5 |
| FastAPI | 10 |
| Next.js | 6 |

**Goだけが空フォームだった。** 他4実装は既定で保存済みの検査を出していた。
契約が決めていない場所で、5実装のうち1つが違う道を選んでいた。

### 裁定

**既定は「その患者の最新の保存済み検査」。** 空の新規フォームは明示したときだけ出す。

理由は2つ。

1. **カルテ画面と揃う。** カルテは既定が最新の診察で、空フォームは別経路（`/karte/new`）。
   同じ考え方を検査にも当てる
2. **既定が空だと、検算5（基準の外にある値は判定欄と色の両方に出る）が
   既定表示では永久に確かめられない。** 画面を開いた人がまず見るものに
   何も出ないのでは、その規則が働いているかどうかを誰も確かめられない

`spec/openapi.yaml` にこの既定を書き足す。**契約が決めていなかったから割れた**ので、
決めて書く。

### 教訓

**契約が何も言っていない場所は、実装ごとに割れる。** そして
**割れていること自体は、どの検査にも引っかからない**（どちらも200を返すため）。

    契約の「書いていないところ」は、書いてある間違いより見つけにくい。


---

## R-25 追跡されていたビルド済みバイナリは、履歴に残したまま追跡だけ外す（2026-09-06・オーナー判断）

**経緯**: 指揮役が Go をビルドしたとき、`stacks/go/clinicops.exe`（14.2MB）を
誤って git の追跡下に入れ、**3コミットぶん公開リポジトリの履歴に入った**。
記録の更新を担当したレーンが、行数を数える過程で気づいた
（素朴に数えると **65,487行**の水増しになる）。

**判断**: オーナーの判断で **そのまま**とする。追跡からは外し、`.gitignore` に加えた。

理由:

1. **秘密情報ではない。** このリポジトリ自身のソースをビルドしたもので、公開上の危険は無い
2. **履歴から消すにはリポジトリの作り直しが要る。** この企画では一度それをやっている
   （合成データの姓がNGワードと衝突したとき）。あのときは**消さなければ公開できない**
   理由があった。今回は無い
3. **作り直すと、コミット履歴という記録そのものが失われる。**
   この企画では、間違いの記録が成果物の一部である

`.git` は 26.9MB。容量が増えるだけで実害は無い。

### 一般化して残すこと

**「消せるか」ではなく「消さなければならないか」で決める。**
履歴の書き換えは、消さなければ公開できないものにだけ使う。
それ以外は、**残したまま追跡を外して、なぜそうしたかを書く**ほうが良い。
記録は消すと戻らない。
