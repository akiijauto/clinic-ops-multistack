"""レーンごとの起動文面を作る。

同じ文面を複数のCLIへ貼ると、各CLIが自分のレーンを知らずに起動する。
2026-09-04 の前回の並列開発で実際に起き、割当が二転三転した。
**1文目に必ずレーン名を入れる**のがこの生成器の目的。

内容を変えるときはこのファイルを直して流し直す。
briefs/ を手で編集しない（5つのうち1つだけ直し忘れる）。
"""
import io
import os

LANES = [
    ('a', 'A', 'Go', 'stacks/go',
     'Go 1.27。**ルーティングのライブラリを使わず `net/http` だけ**で書く。画面は `html/template`。',
     'フレームワークが肩代わりしている部分を自分で書くことになるので、'
     '**何を自分で書いたか**を後で語れるように記録すること。'),
    ('b', 'B', 'Ruby on Rails', 'stacks/rails',
     'Rails 8.1.3.1。**Railsの流儀に素直に従う**。画面は ERB / Hotwire。',
     'Railsが用意している仕組み（バリデーション・ルーティング・ビュー）を使い切ること。'
     '無理に他レーンへ合わせて書き方を変えない。'),
    ('c', 'C', 'PHP / Laravel', 'stacks/laravel',
     'PHP 8.4.24 + Composer 2.10.3。**Laravelの流儀に従う**。画面は Blade。',
     'PHPの落とし穴を踏みやすい。**既定引数は引数を省略したときしか効かない**、'
     '`json_encode(5.0)` は `5` に正規化される、文字数は `mb_strlen` で数える'
     '（2026-09-05 に別プロジェクトで実測した3件）。'),
    ('d', 'D', 'Python / FastAPI', 'stacks/fastapi',
     'Python 3.14.3。FastAPI + Jinja2。',
     'Pydanticで入力の型を厳しく定義できる。**どこまでを型で弾き、どこからを業務の検証にするか**の'
     '線引きを意識して書き、後で語れるようにすること。'),
    ('e', 'E', 'TypeScript / Next.js', 'stacks/nextjs',
     'Node 24.16.0 + npm 11.13.0。Next.js（App Router）+ React。',
     'フレームワークが最も多くを肩代わりする。**肩代わりされている中身**'
     '（ルーティング・入力の検証・状態）を意識して、他レーンとの差を語れるようにすること。'),
]

TEMPLATE = '''# レーン{L}（{stack}）への起動文面

**あなたはレーン{L}です。担当は {stack}、所有ディレクトリは `{dir}/` です。**

他のレーンが同時に動いています。**自分の所有ディレクトリの外を書かないでください。**

## 最初に読むもの（この順で）

1. `coordination/ASSIGNMENT.md` — 割当の唯一の正
2. `coordination/PROTOCOL.md` — 守るルール
3. `coordination/DECISIONS.md` — **迷ったときの答えはここにある**
4. `spec/openapi.yaml` — HTTPの契約（**これが正**）
5. `spec/acceptance.md` — 画面と数値の受け入れ条件

## あなたの仕事

動物病院の窓口業務システム **全26画面**を {stack} で実装し、
**共通テスト（`tests/`）を緑にする**。

{how}

{note}

## 一人で26画面を抱えないこと

**あなた自身がサブエージェントを並列に走らせてください。** 領域は5つに分かれています。

| 領域 | 画面 |
| --- | --- |
| 1 受付・患者 | 一覧 / 新規 / 顧客 / 検索 / 来院履歴 / 削除 / 折りたたみ表示 / トップ |
| 2 診療 | カルテ / 検査 / 投薬 / 予防 / 書類 |
| 3 会計・売上 | 会計 / 会計履歴 / DM / **売上集計（新）** |
| 4 入院・予約・業務 | 入院 / **予約（新）** / ToDo / スタッフ |
| 5 設定 | 設定 / 機能設定 / 取込 / マスタ / このシステムについて |

**ただし、統合点（各領域をつなぐ部分・共通のモデル・レイアウト）はあなた自身が書いてください。**
サブエージェント同士に統合させると、境界の認識がずれて後で合わなくなります。

サブエージェントへ渡すときは、**領域ごとに別の指示文**を書いてください。
同じ文面を複数へ渡すと、自分の担当を知らないまま動き始めます
（2026-09-04 に別プロジェクトで実際に起きた事故です）。

## 共通テストの走らせ方

```bash
python tests/run.py http://127.0.0.1:<自分のポート>
python tests/run.py http://127.0.0.1:<自分のポート> --only smoke
```

**Python で書いてある。** 依存は無い（標準ライブラリだけ）。
あなたのスタックが Python でなくても、**これを走らせる権限は与えてある**。

**「自分で書いたテストが緑」は完了ではない。** これが緑になって初めて完了。

## 完了の条件

次のすべてを満たしたときだけ「終わった」と言えます。

1. **共通テスト（`tests/`）が緑**。自分で書いたテストが緑でも完了ではありません
2. `spec/acceptance.md` の**検算がすべて一致**する
3. **テストの期待値を実装に合わせて書き換えていない**
4. 秘密情報・実データが入っていない

## 守ること

- **git を触らない。** commit / push / checkout は指揮役の仕事です
- **`spec/` と `tests/` を変更しない。** 凍結されています
- **他のレーンと直接やり取りしない。** 契約は `spec/` だけです
- **テストの期待値を実装に合わせて書き換えない。** 疑わしいときは質問してください

## 質問のしかた

| 状況 | 経路 |
| --- | --- |
| 答えが無いと**実装が止まる** | 指揮役へ直接メッセージ |
| 止まらない | `coordination/qa/lane-{l}.md` に書いて**先へ進む** |

**待たないでください。** 止まらない質問は自分で仮決めして進み、
**仮決めしたことを `qa/lane-{l}.md` に書いて**ください。

進捗は `coordination/status/lane-{l}.md` に書いてください。
指揮役はそこと共通テストの結果で状況を把握します。

## いまやること（契約はまだ凍っていません）

**`spec/` と `tests/` はまだありません。** 指揮役が作っている最中です。

待たずに、**契約に依存しない土台**を先に作ってください。ここは仕様が決まっていなくても進みます。

1. `stacks/{dirbase}/` にプロジェクトの雛形を作る
2. 依存を入れる（**このリポジトリの中に閉じること**。新しい実行環境の導入はしない）
3. テストの走らせ方を整える（1件も無くてよい。**走る形**にしておく）
4. `GET /healthz` だけ作る。`{{"status":"ok"}}` を返すだけでよい
   （**`/health` ではない。** 共通テストが叩くのは `/healthz`）
5. 題材を読む。（題材のシステムの資料） に
   **これから何が出来るようになるか**が書いてあります（読むだけ。**このリポジトリを一切変更しないこと**）

**まだ画面を作らないでください。** 契約が凍る前に作ると、凍った契約と食い違って作り直しになります。

土台ができたら `coordination/status/lane-{l}.md` に書いて、指揮役の合図を待ってください。

## 契約が凍ったあとの最初のマイルストーン

**共通テストの1件目を通してください。** 全部ではありません。
ここで全レーンの足並みが揃うことを確認してから、本格的な実装に入ります。
'''

REVIEWER = '''# レーンR（読むだけの役）への起動文面

**あなたはレーンRです。実装しません。読んで、見つけたものを書き出す役です。**

他の5レーン（Go / Rails / Laravel / FastAPI / Next.js）が同時に実装しています。
**どのレーンのコードも直さないでください。** 直すのは担当のレーンです。

## なぜこの役があるのか

指揮役が5レーンの中身を全部読むのは無理だからです。
2026-09-04 の別プロジェクトでは、見つかった間違い12件のうち
**6件はこの見方でしか出ませんでした**。テストは通っていたのに、です。

## 見るのは3つだけ

### 1. 画面に嘘が無いか

「できます」と書いてあって出来ていないもの。押しても何も起きないボタン、
表示されているのに反映されない設定、リンク先が存在しない導線。

### 2. 緑のまま壊れていないか

**テストをわざと壊して、ちゃんと落ちることを確かめてください。**
落ちないテストは、無いのと同じです。

**ただし本体を壊さないでください。** 確かめ方はこうします。

```bash
# 使い捨ての複製を作る（本体は触らない）
git worktree add ../_r-sandbox HEAD
cd ../_r-sandbox
# ここでだけ実装や期待値を壊し、テストが落ちることを見る
python tests/run.py http://127.0.0.1:<対象のポート>
# 終わったら捨てる
cd - && git worktree remove --force ../_r-sandbox
```

`git worktree` と各スタックのテスト実行は**あなたに許可してあります**。
複製の中で何をしても本体には影響しません。**捨てるまでがひと組**です。

「全部緑です」という報告を受けたら、そのテストが**本当に何かを見張っているか**を疑ってください。
期待値が実装に合わせて書き換えられていないかも見ます。

### 3. 仮決めが仮決めと分からない形で紛れていないか

この計画では「止まらない質問は仮決めして進む」ことにしています（`coordination/PROTOCOL.md`）。
**仮決めが記録されないまま実装に溶けると、後から誰も気づけません。**

`coordination/qa/` に書かれていない判断が、コードの中で行われていないかを見てください。

## 書き出し方

`coordination/review/` に書きます。1件につき、次を書いてください。

- **どこで**（ファイルと行）
- **何が**（見たままの事実。「たぶん」は書かない）
- **なぜ問題か**（放っておくと何が起きるか）
- **どのレーンの担当か**

**直さないでください。** 直すのは担当のレーンです。あなたが直すと、
担当が「自分は直していないのに緑になった」状態になり、次から同じ間違いを繰り返します。

## いまやること（契約はまだ凍っていません）

他のレーンはまだ土台を作っている段階です。見るものがまだありません。

先に **計画そのものを読んで、穴を探してください。**

1. `coordination/PLAN.md` `PROTOCOL.md` `DECISIONS.md` `ASSIGNMENT.md` を読む
2. **矛盾・抜け・決まっていないのに決まったように書いてある箇所**を探す
3. 特に `DECISIONS.md` を厳しく見てください。この文書の役割は
   **「実装中にオーナーへ質問しなくて済むように、先に全部決めておく」**ことです。
   **決め落としがあると、そこで止まります。** 「これは決まっていない」を見つけるのがいまの仕事です
4. 見つけたものを `coordination/review/` に書く

**直さないでください。** 指揮役へ渡します。

## 守ること

- **git を触らない**
- **どのレーンのコードも変更しない**
- **`spec/` と `tests/` を変更しない**
- 断定する前に確かめる。**「動いていない」と書く前に、実際に動かしてください**
'''

here = os.path.dirname(os.path.abspath(__file__))
out = os.path.join(here, 'briefs')
os.makedirs(out, exist_ok=True)

for l, L, stack, d, how, note in LANES:
    body = TEMPLATE.format(l=l, L=L, stack=stack, dir=d, dirbase=d.split('/')[-1], how=how, note=note)
    path = os.path.join(out, 'lane-%s.md' % l)
    io.open(path, 'w', encoding='utf-8', newline='\n').write(body)
    print('  briefs/lane-%s.md  レーン%s / %s' % (l, L, stack))

io.open(os.path.join(out, 'lane-r.md'), 'w', encoding='utf-8', newline='\n').write(REVIEWER)
print('  briefs/lane-r.md  レーンR / 読むだけの役')

print('\n生成した文面の検査:')
for l, L in [(x[0], x[1]) for x in LANES] + [('r', 'R')]:
    first = io.open(os.path.join(out, 'lane-%s.md' % l), encoding='utf-8').read().split('\n')[2]
    ok = ('レーン%s' % L) in first
    print('  lane-%s: 1文目にレーン名 %s' % (l, 'OK' if ok else '★なし'))
