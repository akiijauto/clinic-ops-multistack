# レーンR 書き出し #1 — 計画そのものの穴

- 書いた日時: 2026-09-05 17:40〜17:55（実測はこの時刻時点のもの）
- 見た範囲: `coordination/PLAN.md` `PROTOCOL.md` `DECISIONS.md` `ASSIGNMENT.md`
  `briefs/*.md` `make_briefs.py` `scripts/launch-lanes.sh` `spec/README.md` `README.md`、
  および各レーンが 17:55 時点までに置いた現物
- **直していません。** 直すのは担当のレーンと指揮役です

重い順に並べました。**R-01〜R-04 は、いま直さないと5実装ぶん同じ間違いになります。**

---

## R-01 全レーンの起動文面が「24画面」で、売上集計が領域表から落ちている 【重い】

**どこで**

| 場所 | 記述 |
| --- | --- |
| `coordination/PLAN.md:38` | 「画面の内訳（5領域・**26画面**）」。領域3に「**売上集計（新）**」がある |
| `coordination/PLAN.md:18` | 「**全24画面**を、5つの技術スタックで各1実装」 |
| `coordination/briefs/lane-a.md:17`（b/c/d/e も同文） | 「**全24画面**を … 実装し」 |
| `coordination/briefs/lane-a.md:32`（b/c/d/e も同文） | 「3 会計・売上 \| 会計 / 会計履歴 / DM」— **売上集計が無い** |
| `coordination/make_briefs.py:53,68` | 生成元。ここが 24 と「売上集計なし」で固定されている |
| `spec/README.md:10,26,80` | 「**26画面**」 |
| `README.md:3`（公開物） | 「**全24画面**」 |

**何が**

- 契約側（`spec/README.md`）は 26画面、レーンへ配った起動文面は 24画面
- 起動文面の領域表に並んでいる画面を数えると **25枚**（8+5+3+4+5）。本文の「24画面」とも、PLANの26枚とも合わない
- 新規2画面のうち **予約は表に載っているが、売上集計はどこにも載っていない**
- `make_briefs.py` は「briefs/ を手で編集しない（5つのうち1つだけ直し忘れる）」ために作られているので、**生成元を直さないと5本とも直らない**

**なぜ問題か**

各レーンは起動文面の領域表をサブエージェントへの分担表に使います（`briefs/lane-a.md:26`）。
このまま進むと **5実装すべてで売上集計が作られません**。
売上集計は題材側で「常設ナビの灰色3個」の1つであり（`vet-karte/docs/実装分担-2026-09-05.md` の PR #4）、
**新規に作ると決まっている2画面の片方**です。
第3段階の突き合わせで5実装そろって無いと、「無いこと」自体に気づけません。

**担当**: 指揮役（`make_briefs.py` を直して再生成 → 各レーンへ通知）

---

## R-02 ログイン／ログアウトを「作る」と「作らない」が両方書いてある 【重い】

**どこで**

- `coordination/PLAN.md:66` — 動くようになる38個の例として「絞り込み・上下送り・診察券の発行・番号の変更・CSVの書き出し・**ログイン**」
- `coordination/DECISIONS.md:64` — 「認証 \| **この計画では扱わない。** 画面はログイン済みの前提で作る」

**何が**

同じ計画の中で、ログインが**作る対象に数えられ**、同時に**扱わないと決められて**います。
題材側では認証は独立した受け持ち（`実装分担-2026-09-05.md` PR #8「ログイン／ログアウト」、効く数3）で、
設定タブの「パスワード変更・管理者設定」もそこに連なります。

**なぜ問題か**

`DECISIONS.md` は「実装中にオーナーへ聞かなくて済むように先に全部決めておく」文書です。
**そこと PLAN が逆を向いていると、レーンは決められません。**
実際に止まるのは領域4（スタッフ）と領域5（設定）で、5レーン同時に止まります。
また「38個を動かす」という数字も、ログイン／ログアウトを外すなら **38 ではなくなります**。
数字を根拠に語る計画なので、ここは数字ごと決め直しが要ります。

**担当**: 指揮役（`DECISIONS.md` 第4節に「認証は作らない。38個から3個を除いて35個」等、数まで含めて書く。決めた結果は `qa/` へ通知）

---

## R-03 5レーンの待ち受けポートが決まっておらず、Rails と Next.js が既定で衝突する 【重い】

**どこで**（17:55 時点の実測）

| レーン | ポート | 根拠 |
| --- | --- | --- |
| A Go | `:8080` | `stacks/go/internal/config/config.go:36` |
| B Rails | `3000` | `stacks/rails/config/puma.rb:32`（`ENV.fetch("PORT", 3000)`） |
| D FastAPI | `8004` | `stacks/fastapi/run.py:12`（レーンDの仮決め） |
| E Next.js | `3000` | `next dev` の既定。`package.json` に指定なし |
| C Laravel | 未着手 | `stacks/laravel/` はファイル0個 |

**何が**

`PLAN.md` `DECISIONS.md` `spec/README.md` のどこにも**ポートの割当がありません**。
各レーンが別々に仮決めしており、**BとEはすでに同じ3000**です。
（`php artisan serve` の既定は8000なので、Cが既定のまま来ると 8000 番台でDと近接します）

**なぜ問題か**

共通テスト（`tests/`）は HTTP 越しに5実装を叩く設計です（`PLAN.md` 成立条件）。
**judge が5つの宛先を知る手段が無く、しかも2つが同じ番号**では、第1段階の「共通テストの1件目を通す」がそもそも成立しません。
5実装を**同時に立てて突き合わせる**第3段階では確実に踏みます。

これは `DECISIONS.md` 第6節でいう「上の原則から導けない事柄」＝**契約の欠陥**に当たります。

**担当**: 指揮役（`spec/` にポート表を1つ置く。例: A=8081 B=8082 C=8083 D=8084 E=8085。`BASE_URL` を環境変数で受ける形にすると judge 側から差し替えられます）

---

## R-04 共通テストを、レーン自身が走らせられない 【重い】

**どこで**

- `scripts/launch-lanes.sh:47-55` — 各レーンに許可した Bash は自分のスタックの道具だけ（A: `go`/`gofmt`、B: `ruby`/`bundle`/`rails`/`rake`、C: `php`/`composer`、D: `python`/`pip`/`pytest`/`uvicorn`、E: `node`/`npm`/`npx`）
- `coordination/DECISIONS.md:70` / 各 brief の完了条件1 — 「**共通テストが緑**。自分で書いたテストが緑でも完了ではない」

**何が**

共通テスト（`tests/`）を何で書き、どう起動するかが**どこにも決まっていません**。
仮に Python（pytest）で書くと、**それを走らせられるのはレーンDだけ**です。Node で書けばEだけになります。
`curl` はどのレーンにも許可されていません。

**なぜ問題か**

完了条件が「共通テストが緑」なのに、**レーンには緑を確かめる手段がありません**。
確かめられないと、レーンは「自分のテストが緑」で完了を報告し、
`PROTOCOL.md:8`（自己申告は引き金にすぎない）を運用でしか支えられなくなります。
第2段階の終盤で5レーンぶん同時に発覚します。

**担当**: 指揮役（共通テストの言語と起動方法を `spec/` に書き、各レーンの許可に**その実行手段だけ**足す。全許可にはしない）

---

## R-05 起動スクリプトの `--allowedTools` が語分割で壊れている 【中】

**どこで**: `scripts/launch-lanes.sh:83,90`

```
tools="$COMMON_TOOLS ${LANE_TOOLS[$l]}"
...
--allowedTools $tools     # ← 引用していない
```

**何が**（実測。同じ書き方を切り出して走らせた結果）

```
引数の個数: 12
  [9] <Bash(go>      ← Bash(go *) が2つに割れている
  [10] <*)>
  [11] <Bash(gofmt>
  [12] <*)>
```

`Bash(go *)` のような**空白を含む1項目**が、`Bash(go` と `*)` の2項目に分かれて渡ります。
意図した許可はどのレーンにも渡っていません。レーンRの `Bash(git log *)` も同様です。

なお **いま全レーンが止まっているわけではありません。**
17:55 時点で D は `.venv` を、E は `node_modules` を、B は Rails 一式を作れているので、Bash は別の経路で通っています。
**壊れているのは渡した値であって、現時点の症状ではありません。**

**なぜ問題か**

このスクリプトのコメント自身が「許可しておかないと、背景で動いているレーンが確認待ちで止まる（**止まったことに気づけないのが最悪**）」と書いています。
いまたまたま通っているだけで、**許可の設計は効いていません**。
同時に「インストールや git は許さない」という歯止めも、同じ理由で意図どおりには効いていません。

**担当**: 指揮役（配列で渡す。例: `tools=($COMMON_TOOLS "${LANE_TOOLS_ARR[@]}")` として `--allowedTools "${tools[@]}"`。直したら DRY_RUN で1レーンぶん出力を目で確かめる）

---

## R-06 レーンR（私）への指示が、実行不可能な形になっている 【中】

**どこで**: `coordination/briefs/lane-r.md:23` と `:67`、`scripts/launch-lanes.sh:55`

**何が**

- 23行目「**テストをわざと壊して、ちゃんと落ちることを確かめてください**」
- 67行目「**`spec/` と `tests/` を変更しない**」、加えて冒頭で「どのレーンのコードも変更しない」
- 許可されている Bash は `git log` / `git diff` / `git show` の3つだけで、**テストランナーが1つもありません**

見張りが鳴るかを確かめる方法は「テストを書き換える」か「実装を壊す」かのどちらかですが、**両方とも禁止**されており、そもそも**走らせる道具がありません**。

**なぜ問題か**

「緑のまま壊れていないか」は、この役に与えられた3つのうちの1つです。
いまの形では、その1つが**構造的に実行できません**。
できないまま「見た」と報告すると、**見張りが鳴らないことに誰も気づけない**状態が残ります。

**どうすれば通るか（提案。決めるのは指揮役）**

- 読み取り専用の複製（`git worktree` か一時ディレクトリへの copy）を1つ用意し、**そこでだけ**壊して鳴ることを確かめ、複製は捨てる。本体は触らない
- レーンRに各スタックのテスト実行だけ許可する（`go test` / `pytest` / `npm test` / `rails test` / `php artisan test`）

**担当**: 指揮役（`make_briefs.py` の REVIEWER と `launch-lanes.sh` の許可）

---

## R-07 コードに「`qa/` に記録した」と書いてあるが、`qa/` は空 【中】

**どこで**: `stacks/fastapi/run.py:4`

```
契約に指定が無いので **8004** を仮決めした。`coordination/qa/lane-d.md` に記録。
```

**何が**

17:49 と 17:55 の2回、`coordination/qa/` を確認しました。**空です**（ファイル0個）。
`stacks/go/internal/store/doc.go:16` にも「決めたら `coordination/qa/lane-a.md` に理由とともに書く」とあり、こちらも未記入です。

**なぜ問題か**

この計画は「止まらない質問は仮決めして進む。**仮決めしたことを `qa/` に書く**」で回っています（`PROTOCOL.md:46,49`）。
**「記録した」と書いてあって記録が無い**のは、記録が無いことより悪いです。
指揮役が `qa/` を見て「仮決めは無い」と読み、コード側は「記録済み」と読むためです。
仮決めが実装に溶けたまま誰も気づけなくなる、この役が見るべき3つ目そのものです。

**担当**: レーンD（`qa/lane-d.md` を実際に書く）／レーンA（決めたら書く）。
併せて指揮役は、**各レーンが `qa/` へ1行も書かないまま進んでいないか**を定期的に見てください。

---

## R-08 `DECISIONS.md` に、決めきれていない事柄が複数残っている 【中】

`DECISIONS.md` の役割は「実装中にオーナーへ質問しなくて済むように先に全部決めておく」ことです。
**題材側で「動かす」と決まっている38個を実装するのに、いま答えが出ない**ものを挙げます。
（38個の内訳は `vet-karte/docs/実装分担-2026-09-05.md` の1節・3節を実測して照合しました）

| 決まっていない事柄 | どこで要るか | いまの `DECISIONS.md` で答えが出るか |
| --- | --- | --- |
| **消費税**（税率・内税/外税・端数処理） | 病院設定の未実装欄6個の1つ。会計の「金額と支払い」 | 出ない。「丸めは `acceptance.md` に無ければ丸めない」では税額が決まらない |
| **ポイント**の計算規則 | 同上6個の1つ | 出ない |
| **最終伝票番号／機関コード**の採番規則 | 同上 | 出ない |
| **診察券の発行**（番号の形式・採番） | 顧客の6個の1つ | 出ない |
| **CSVエクスポートの文字コード**（UTF-8 / BOM / Shift_JIS） | 設定タブ5個の1つ | 出ない。「文字数はバイトでなく文字で数える」は別の話 |
| **削除の意味**（論理削除か物理削除か） | 飼主削除・動物削除・削除画面 | 出ない |
| **休診日**の持ち方 | 病院設定。予約画面が参照する | 出ない |
| **レセプト／オプション** | 設定タブ2個 | 出ない。範囲外にするなら明記が要る |

**なぜ問題か**

どれも「導けない＝契約の欠陥」（`DECISIONS.md:96` 第6節の3）に当たります。
5レーンが**別々に仮決めすると、5通りの振る舞い**ができます。
共通テストは書かれた条件しか見ないので、**テストは全部緑のまま、突き合わせで初めて割れます**。
第3段階（14〜18時間）を押し上げるのはここです。

**担当**: 指揮役（`spec/acceptance.md` に数値の規則として1つずつ書く。範囲外にするものは `spec/README.md` の「何を落としたか」へ理由つきで載せる）

---

## R-09 マスタ「編集は作らない」と、設定の灰色ボタン11個を動かすことが、線引きなしに同居 【中】

**どこで**: `spec/README.md:28`「マスタ管理の画面 \| **一覧と参照は作る。編集は作らない。**」

**何が**

題材側で「動かす」と決まっている38個には、**病院設定の未実装欄6個**（消費税・ポイント・最終伝票番号・機関コード・ロゴ・休診日）と**設定タブ5個**が含まれます。
これらは値を**保存する**機能なので、「編集は作らない」と重なります。
どこまでがマスタ編集で、どこからが設定の保存なのかが書かれていません。

**なぜ問題か**

5レーンが別々に線を引きます。
さらに `spec/README.md:35` は「**画面に『できます』と書いてあって出来ていない状態を作らない**」と明記しているので、
**線引きを書かないまま入力欄だけ置くと、その禁止に自分で抵触します**。

**担当**: 指揮役（`spec/screens.md` に画面単位で「保存できる／表示だけ」を明記）

---

## R-10 `ASSIGNMENT.md` のセッション対応表が空欄のまま 【中】

**どこで**: `coordination/ASSIGNMENT.md:45-51`（全行「（起動時に記入）」）／`coordination/launched.md`

**何が**

`ASSIGNMENT.md` は「セッション同士の直接メッセージ、CLI起動時のメニュー選択、作業中のファイル、タイムスタンプの**いずれよりも優先する**」唯一の正と定義されています。
その表が空欄で、**実際のセッションIDは自動生成の `launched.md` にしかありません**。

**なぜ問題か**

指揮役が「どのセッションがどのレーンか」を知る唯一の場所が、`ASSIGNMENT.md` ではなく別ファイルになっています。
`PROTOCOL.md:7`（自己申告・ロック・タイムスタンプから割当を逆算しない）が防ごうとした事故は、**唯一の正が識別情報を持っていない**とまた起きます。2026-09-04 の再発条件そのものです。

**担当**: 指揮役（`launched.md` の6行を `ASSIGNMENT.md` へ転記するか、`ASSIGNMENT.md` から `launched.md` を正として参照すると明記する）

---

## R-11 「所有ディレクトリの外を書かない」と「`status/` `qa/` に書け」が正面から矛盾 【低〜中】

**どこで**: `ASSIGNMENT.md:19`「各レーンの作業は `stacks/<自分の名前>/` の中で完結する」／`PROTOCOL.md:10`（不変条件3）／各 brief「`coordination/qa/lane-x.md` に書いて」「`coordination/status/lane-x.md` に書いて」

**何が**

不変条件として「所有ディレクトリの外を書かない」と書かれた上で、`coordination/` 配下の2ファイルへ書くことが求められています。

**なぜ問題か**

ルールに忠実なレーンほど `status/` `qa/` への書き込みをためらいます。
実際 17:55 時点で `status/*.md` は6本とも見出し1行のままで、内容がありません。
指揮役は `status/` と共通テストで全体を把握する設計（`PROTOCOL.md:53`）なので、**把握の口が塞がったまま**になります。

**担当**: 指揮役（所有を「`stacks/<自分>/` ＋ `coordination/status/lane-<自分>.md` ＋ `coordination/qa/lane-<自分>.md`」と書き換える）

---

## R-12 `DECISIONS.md` に、この企画のものではない記述が残っている 【低】

**どこで**: `coordination/DECISIONS.md:45`

```
| AIのAPI呼び出し | **アプリからは呼ばない。** この3画面にAI機能は無い |
```

**何が**

この企画の画面は26枚（または24枚。R-01参照）で、**3画面ではありません**。別案件の文面がそのまま残っています。

**なぜ問題か**

`DECISIONS.md` は「ここに書いてあることは決定済みであり、蒸し返さない」文書です。
**その中に事実でない記述が混じると、他の行の信頼も落ちます。**
決定そのもの（アプリからAIを呼ばない）は妥当なので、根拠の1行だけ直せば済みます。

**担当**: 指揮役

---

## R-13 第2段階の見積もりが、自分で書いた但し書きと合っていない 【低】

**どこで**: `coordination/PLAN.md:186-198`

**何が**（PLANに書かれている数字だけで計算しました）

| 前提 | 1スタック 26,000行 | 33,000行 |
| --- | ---: | ---: |
| RetailBrain の 1,080行/時 | 24.1時間 | 30.6時間 |
| PHP移植の 400行/時 | 65.0時間 | 82.5時間 |
| 両者の中間 740行/時 | 35.1時間 | **44.6時間** |

PLANの見積もりは **28〜38時間**で、これは 1,080行/時 側にほぼ収まる値です。
一方でPLAN自身が「実際は**両者の間に落ちる見込み**」と書いています。
**中間を採ると上限44.6時間で、38時間を超えます。**

**なぜ問題か**

「順に作れば140〜190時間、並列で55〜73時間」という主張は、この段階の見積もりから出ています。
**この計画の売りは倍率を実測で語ることなので、根拠が自分の但し書きと食い違うと、そこを突かれます。**
（CLAUDE.md「倍率を言うときは必ず基準を添える」）

**担当**: 指揮役（範囲を 28〜45時間程度へ広げるか、「1,080行/時 を前提とした値である」と but書きを添える）

---

## R-14 レーンE: `npm test` は既定で緑になるが、HTTPを見ているテストは skip される 【中・レーンE】

**どこで**: `stacks/nextjs/test/health.http.test.ts:10` / `stacks/nextjs/package.json:14`

**何が**（17:49 実測。`npm test` を実際に走らせました）

```
﹣ GET /health returns {"status":"ok"} (0.4833ms) # set BASE_URL to run
✔ health() returns the documented payload (0.8581ms)
ℹ pass 1  ℹ fail 0  ℹ skipped 1
```

- HTTP を叩くテストは `BASE_URL` が無いと skip され、**それでも `fail 0`＝緑**
- 通ったほうの1件は `src/lib/health.ts` の `health()` を呼ぶだけで、**`src/app/health/route.ts`（実際の経路）を1行も通りません**
- `BASE_URL` を設定する仕組みはレーン内のどこにもありません

**なぜ問題か**

いまのレーンEの緑は「`{status:'ok'}` を返す関数が `{status:'ok'}` を返す」ことしか見張っていません。
**`/health` の経路を消しても、この `npm test` は緑のままです。**
第1段階のマイルストーンは「共通テストの1件目が通ること」なので、ここで緑を根拠にすると、通っていないものを通ったと数えます。

なお、skip する理由は当人がコメントで説明しており、**隠してはいません**。問題は隠蔽ではなく、**既定が緑になること**です。

**担当**: レーンE（`BASE_URL` 未設定を skip ではなく fail にする、または起動込みで叩く形にする。決めたら `qa/lane-e.md` へ）

---

## R-15 レーンE: `package.json` の `seed` が指すファイルが無い 【低・レーンE】

**どこで**: `stacks/nextjs/package.json:15` — `"seed": "node scripts/seed.ts"`

**何が**: 17:55 時点で `stacks/nextjs/scripts/` は**存在するが空**（ファイル0個）。`npm run seed` は失敗します。

**なぜ問題か**: 「できます」と書いてあって出来ていないものの、最小の形です。
いまは着手直後なので、**これから置くのであれば問題ありません**。置かないなら宣言を消してください。放っておくと、公開時にそのまま残ります。

**担当**: レーンE

---

## 補足（欠陥ではないが、指揮役に知らせておくもの）

- **`data/` がまだありません。** `README.md` の構成にも `PLAN.md` 第0段階4にもありますが未作成です。
  無いまま各レーンが自前のシードを作ると、突き合わせ段階で入力そのものが違うことになります。
  第0段階の途中なので**いまは想定どおり**ですが、**契約凍結より先に置く必要があります**
- **`spec/` にあるのは `README.md` だけ**で、`model.md` `screens.md` `acceptance.md` `openapi.yaml` はまだありません（`spec/README.md:8-12` が読む順として挙げているもの）。
  各レーンの起動文面は「4. `spec/openapi.yaml`（**これが正**）」を最初に読むよう指示しているので、レーンは**存在しないファイルを読もうとします**。いまは「まだありません」と別途書かれているので事故にはなっていません
- **PLANの実測値は正しいものが多い**です。確かめた範囲では次が一致しました。
  題材の `app/` の Python 行数 = **15,700行**（PLAN `:58` と一致）、
  画面テンプレート = 部分テンプレートと `base.html` を除いて **24枚**（PLAN `:56` と一致）、
  灰色ボタン **41個・作らない3個**（`実装分担-2026-09-05.md` 1節と一致）
- ただし26画面の内訳（`PLAN.md:40-48`）には、テンプレートを持たない「トップ」（`/` は `about` を出すか `/today` へ転送するだけ。`vet-karte/app/main.py:715`）が1枚として入り、
  代わりに **`print/karte.html`（印刷用カルテ）が抜けています**。
  38個の中には「文書印刷」（顧客）と「印刷」（会計）が含まれるので、**押した先の画面が領域表に無い**状態です。R-01 を直すときに併せて見てください
