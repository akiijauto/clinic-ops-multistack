# 横並び再測（統括・2026-09-06 04:40）

**5実装すべてを起動して `compare.py` を回した。食い違いが 3 件ある。**
公開中のリポジトリは緑のままで、**この3件は未コミットの作業分で入った戻り（regression）**である。

## 測った条件

| レーン | スタック | ポート | `/healthz` |
| --- | --- | ---: | --- |
| A | Go | 8401 | 200 |
| B | Rails | **8414** | 200 |
| C | Laravel | 8403 | 200 |
| D | FastAPI | 8415 | 200 |
| E | Next.js | 8405 | 200 |

## 食い違い 3 件

| # | 検査 | A | B | C | D | E | 中身 |
| --- | --- | :-: | :-: | :-: | :-: | :-: | --- |
| 1 | 検算2 消費税は伝票単位で1回だけ切り捨て | OK | **NG** | OK | OK | OK | 消費税 **2200**、期待 **2750** |
| 2 | 検算4 カルテの画面と印刷で同じ値が出る | OK | OK | OK | OK | **NG** | `10002` の `progress_note.temper` |
| 3 | 検算8 画面から辿れるリンクが全部生きている | OK | **NG** | OK | OK | OK | 3件が死にリンク（`/folded/hospital_divisi…`） |

## これは判定器の欠陥ではない（見分け方）

これまで判定器の欠陥は **「1つの実装だけが、たくさん落ちる」** 形で出た。
今回は **落ちている実装が2つに分かれ、しかも検査が1件ずつ**。形が違う。

- #1 は **Rails だけが金額を間違えている**。他4実装は期待値と一致
- #2 は **Next.js だけが画面と印刷でずれている**
- #3 は **Rails だけがリンク先を作っていない**

**期待値は `tests/expected.py` が `data/` から独立に計算しており、実装のコードを通していない。**
つまり「4対1で多数決」ではなく、**4つが独立計算と一致し、1つがずれている**。

## 各レーンへ

- **レーンB（Rails）**: 2件。`2200` は期待 `2750` の 0.8倍。
  税率か課税対象行の取りこぼしを疑う。リンク3件は新規画面の配線先が未作成
- **レーンE（Next.js）**: 1件。カルテ `10002` の体温が、画面と印刷で別の値になっている

## 統括からの申し送り（測るときの落とし穴）

1. **`PORTS.md` が実態とずれていた。** Rails は 8402 ではなく **8414**。
   `compare.py` は 8402 を見ていたので **動いているのに「停止」と表示**していた。
   正本と書いてあるファイルも、突き合わせなければ正ではない
2. **Go は `go` が cmd.exe の PATH に無い。** `C:\Program Files\Go\bin\go.exe` を明示する
3. **`go run` を二重に投げると両方落ちる。** 先に `go build` して単一プロセスで起動する