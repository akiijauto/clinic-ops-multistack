# stacks/nextjs/test

Lane E's own tests. **These are not the finish line** — the finish line is the
shared suite in the repository's `tests/` directory (see `coordination/PROTOCOL.md`).

Run with:

    npm test

`node --test` is Node's built-in runner and Node 24 strips TypeScript types
natively, so no test framework is installed.
