// Package store は保存先を受け持つ。
//
// 中身はまだ無い。契約（spec/model.md）が凍結していないので、
// 型を先に作ると凍った契約と食い違って作り直しになるため。
//
// 保存先の選択について、この時点で分かっていること（2026-09-05 実測）:
//
//   - CGO_ENABLED=0、gcc も入っていない。
//     したがって cgo を使う SQLite ドライバ（mattn/go-sqlite3）は選べない。
//   - 純 Go の SQLite（modernc.org/sqlite）は cgo 無しで動くが、
//     モジュールの取得が要る。
//   - 標準ライブラリだけで済ませる案（メモリ上の索引＋JSON ファイルへの保存）もある。
//     追加インストールが要らない点では最も確実。
//
// どれを採るかは、契約が凍って必要な問い合わせの形が見えてから決める。
// 決めたら coordination/qa/lane-a.md に理由とともに書く。
package store
