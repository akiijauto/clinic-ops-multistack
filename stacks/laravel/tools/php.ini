; レーンC 専用の php.ini（tools/setup.ps1 が生成。手で書き換えない）
; 環境の php.ini は変更していない。PHPRC でこのファイルだけを読ませている。
extension_dir = "C:/Users/touro/AppData/Local/Microsoft/WinGet/Packages/PHP.PHP.8.4_Microsoft.Winget.Source_8wekyb3d8bbwe/ext"

extension=curl
extension=fileinfo
extension=mbstring
extension=openssl
extension=sodium
; ここから下が、環境の php.ini では無効になっていたもの
extension=pdo_sqlite
extension=sqlite3
extension=zip

memory_limit = 512M
; 業務が国内の窓口業務なので JST（spec/README.md「日付・時刻」）
date.timezone = "Asia/Tokyo"
